# CONTENT-SEO blog tick — 2026-09-19 (IST)

## Gate

IST post count for 2026-09-19 at tick start: **0**. Cap is 4/day, target 2-3, never 0 —
so a post was mandatory, not optional. Published 1.

## Keyword research

GSC pull, 28 days (2026-08-20 → 2026-09-16), `sc-domain:richdeals.in`:
site total **2 clicks / 67 impressions**. Signals read this tick:

| Query | Impr | Pos |
|---|---|---|
| best perfume for men in winter under 500 | — | 1.0 |
| best perfume under 500 for man | — | 1.0 |
| flats for women under 300 | 3 | 1.0 |
| earbuds under 2000 | — | 1.0 |
| football under 300 | — | 4 |
| freebies | 10 | 85.4 |
| flipkart deals today / today offer / offer today / midnight offer | 63 / 55 / 59 / 68 | — |
| amazon pay later cibil score | 51 | 58 |
| giordano watch price | 9 | — |

**Perfume rejected as a near-dup.** `best-perfumes-under-500-india-2026` and
`best-perfumes-for-men-india-2026` already exist, and the `best-*-under-500-*` cluster is
saturated (fashion deals, kitchen knife sets, wireless mouse, mens shoes, airtight containers,
bluetooth neckband, home toolkit, car body covers). The pos-1 perfume queries are already
covered by live pages — another post there is exactly the dup-slug thin content the audit
keeps flagging.

**Free-samples cluster also rejected.** 20+ `free-sample*` slugs already live, 46 of 314 posts
in that cluster, `where to get free samples` sitting at 11k impressions / pos 6.8 / 0 clicks.
That cluster needs consolidation, not a 21st entry.

**Picked:** uncovered telecom-utility intent. Dup check on `dnd` / `spam` / `telemarket`
returned **NONE** across all 313 existing slugs. Evergreen, high real-world volume, zero
seasonal decay, and nothing on the site competes with it.

## Published

| Field | Value |
|---|---|
| Slug | `how-to-stop-spam-calls-dnd-india-2026` |
| Post id | 373 |
| Title | How to Stop Spam Calls and SMS in India: The Full DND Guide (2026) |
| seoTitle | `How to Stop Spam Calls in India: DND Setup (2026)` — **49 chars** (cap 60) |
| seoDesc | **152 chars** (window 150-160) |
| Body | **1,303 words** (window 900-1600), 7,748 chars stored |
| Tags | dnd, spam-calls, telecom, how-to, india (5) |
| Cover | `https://richdeals.sfo3.digitaloceanspaces.com/og/how-to-stop-spam-calls-dnd-india-2026.png` |
| Alt | `alt={post.title}` at `apps/web/src/app/blog/[slug]/page.tsx:202` — verified |
| In-body images without alt | 0 |
| Internal links | `/4g-5g-wifi-dongle-with-sim-slot-portable-wireless-hotspot-up-to-150-mbps-b0h8p2mc3m` (LIVE deal, ₹2,399) + `/offers` |
| IndexNow | `DONE: 1 inserted. IndexNow -> HTTP 200` — no 422, Bing fallback not needed |

Inserted through `scripts/insert-blog-mdmeta.mjs` (upsert + tags + IndexNow POST), cover via
`scripts/gen-blog-covers.mjs`. No hand-rolled insert, no skipped ping.

### GEO patterns applied

Answer-first opening (the exact SMS command in the first sentence, before any preamble);
question-shaped H2s that map to real queries; a 7-row category table; the 140 / 1600 number-series
distinction as a standalone citable fact; a numbered action sequence; a 5-question FAQ block at
the end. No fabricated stats — every claim is a rule or a procedure, and the two soft ones
("give it a full day", "the complaint window is short") are phrased as guidance, not as invented
numbers.

## Verification after publish

- `post.cover` non-null; `coverless` across all 314 posts = **0**; `seoless` = **0**.
- Posts-per-day IST: 09-19:**1**, 09-18:3, 09-17:3, 09-16:3, 09-15:3, 09-14:4, 09-13:4.
  Never 0, never over 4.
- Both internal link targets confirmed live in the DB before writing.

## CEO audit (verified against the DB this tick)

- Posts **314**. Coverless 0, seo-less 0.
- Deals LIVE **10,229**, PENDING_REVIEW 0, null price 0, null image 0.
- Today IST now at 1 post — **2 more due today** to hit the 2-3 target. Next BLOG tick must not skip.
- **tg-broadcast cursor 10,482 vs max deal id 10,574 = 92 behind.** The external cron is still
  dead. Draining it fires ~92 channel messages in one burst, so it stays parked until the owner
  says go.
- No unpushed commits before this tick.

## Rot list — 17 items, unchanged

1. `apps/api/scripts/lib/ingest-common.mjs` still has no Amazon extractor (20th tick hand-derived).
2. Source-vs-live price drift is the dominant reject reason across every source.
3. indiafreestuff Flipkart links structurally dead.
4. tg-broadcast external cron not firing — cursor drift now 92.
5. `curlFinal` cannot follow client-side redirects (`rogerkart.com/r/…`).
6. ~~`amazn.lt` NXDOMAIN~~ — corrected, transient DNS only, not rot.
7. `.claude/agents/deal-ingest.md` stale on 4 points plus RSS.
8. `where to get free samples`: 11,072 impressions / pos 6.8 / **0 clicks**; 46 of 314 slugs.
9. Organic collapse: last-28d GSC = 2 clicks / 67 impressions.
10. W6 (`/coupons` + `/freebies` ignore `?type=`) and W8 (`sku` needs `productId` on the shared DTO) both need an API change.
11. ~605 untracked scratch files under `apps/api/`.
12. CLAUDE.md documents chunked sitemaps that prod 404s.
13. `link.amazon` shortlinks can expand to `/s?hidden-keywords=` — reject must run post-resolve.
14. Flipkart PDPs serve no ld+json to a real browser either, and a stale tab lies.
15. tg seen-cache and DB disagree in both directions; only the DB check catches it.
16. The Amazon ₹-coupon badge is not extracted by any shared code.
17. indiafreestuff Feedburner RSS dead (HTTP 000); CLAUDE.md and the agent doc both still name it primary.

## Open owner decisions (5, unchanged)

1. Ratify publish-at-verified-live-price + the 30% discount floor in CLAUDE.md, and extend it to
   say coupon-inclusive source prices are reconciled, not rejected.
2. Permanent DB pool cap in `apps/api/.env`.
3. External crons — DesiDime `7,37 * * * *` and tg-broadcast — recreate or retire.
4. Free-samples cluster consolidation (46 of 314 slugs).
5. Scratch-file cleanup under `apps/api/`.
