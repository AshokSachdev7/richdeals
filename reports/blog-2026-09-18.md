# CONTENT-SEO blog — 2026-09-18

## 00:4x IST tick
- Today IST count 0 (09-17 closed at 3) → published 1.
- Post: amazon-pay-later-cibil-score-impact-india-2026 (id 370, 1,453 words).
  seoTitle 52 chars, seoDesc 159, 4 tags, internal links → /offers + /blog/amazon-pay-later-vs-credit-card-india-2026 + /blog/no-cost-emi-explained-india.
  Cover generated + uploaded (HTTP 200), alt = post title via template. Page live HTTP 200.
- IndexNow: HTTP 200 via insert-blog-mdmeta.mjs (no 422, no Bing fallback needed).
- Keyword basis: GSC 28d shows "amazon pay later cibil score" + "amazon pay later eligibility cibil score" at pos 51-58, zero clicks — real impressions, no page targeting them. Not a near-dup: only existing pay-later post is the vs-credit-card comparison.
- Avoided free-samples cluster on purpose: 45 posts already, saturated.

### ROT FLAGGED
- Organic collapse: GSC 90d (Jun18-Sep15) = 50 clicks / 336 impr, but last 28d (Aug19-Sep15) = 6 clicks / 153 impr. Winner page /blog/how-to-get-free-samples-freebies-india fell to pos 14.2 (was ~6.9) with 20 impr / 1 click. The free-samples cluster is decaying, likely self-cannibalised across 45 near-dup slugs. Needs a merge/prune pass, not more posts.
- DB connection slots: local API (:4000) holds slots; audit script died once at 23:48 even with connection_limit=1. Permanent pool cap still unshipped, owner call.

## 06:4x IST tick

- Today IST count was 1 (floor is 2-3) → published 1. Day now at **2**.
- Post: `flipkart-supercoins-earn-redeem-guide-india-2026` (id 371, 8,433 chars / ~1,250 words).
  seoTitle 54 chars, seoDesc 151, 4 tags (flipkart, supercoins, rewards, shopping-guide).
  Internal links → `/offers` + `/blog/flipkart-coupons-offers-india-2026` + `/blog/cashback-vs-coupons-vs-reward-points-india`.
- IndexNow: **HTTP 200** via `insert-blog-mdmeta.mjs` (no 422, Bing fallback not needed).
- Cover generated + uploaded → `https://richdeals.sfo3.digitaloceanspaces.com/og/flipkart-supercoins-earn-redeem-guide-india-2026.png` (HTTP 200). Alt = post title, confirmed in rendered HTML. Page live HTTP 200.

### Keyword basis

GSC 90d pulled at 1000 rows. Every striking-distance query already owns a
dedicated page (vacuum cleaners, boat-vs-noise, student discounts, best-time-to-buy-TV,
home gym, smartwatches under 3000, study chairs, mixer grinders, bedsheets,
lightning deals, price tracking, no-cost EMI). Writing any of those again = the
near-dup padding `dup-slug-thin-content` warns about.

Gap confirmed by grep over all 311 existing slugs: **zero** slugs contain
`supercoin`, `cashkaro`, `coupondunia`, `magicpin`, `open-box` or
`amazon-pay-balance`. Only `cashback-vs-coupons-vs-reward-points-india` matched
`reward-point`, and that is a concept post, not a brand guide.

Chosen for: no near-dup, evergreen (memory forbids seasonal sale phrases, which
rules out the Great Indian Festival / Big Billion Days clusters), India-only,
low competition outside Flipkart's own thin help pages, GEO-friendly definitional
Q&A shape.

**Fact-handling note:** `WebFetch https://www.flipkart.com/supercoin` returned
HTTP 403. Rates resolved from Flipkart's own stories.flipkart.com pages (Plus =
4 coins/₹100 cap 100/order, "double the rate for non-Plus" → 2/₹100 cap 50;
200 coins in 12 months unlocks Plus). Expiry period is contradictory across
sources and Flipkart's official page states none — the post says so explicitly
and points readers at their own Coin Activity screen instead of asserting a
number.

### CEO audit

| check | result |
|---|---|
| posts today IST | 2 (370, 371) — below the 2-3 target's upper half, above the floor |
| published total | 312, **coverless 0, seoless 0** |
| deals PENDING_REVIEW | 0 |
| LIVE deals null price / null image | 0 / 0 |
| broadcast cursor vs DB max | `lastId:10386` = max LIVE id 10386 — caught up |
| prod `/` `/offers` `/blog` `/sitemap.xml` `/feed.xml` `/api/deals` | 200 × 6 |
| unpushed commits | none |
| session crons | all 8 alive (CronList) |

### ROT FLAGGED (carried, still open)

- **`where to get free samples`: 11,072 impressions, position 6.8, 0 clicks (0.0% CTR).**
  That single query dwarfs everything else on the property. Read: our
  India-specific page ranks for a global/US-intent query it cannot satisfy,
  and/or an AI Overview is absorbing the clicks. **This is not fixable by
  publishing more posts** — 46 of 312 slugs are already free-samples/freebies.
  It argues for the cluster merge/prune, which is still the highest-value
  unshipped SEO action on the site.
- Organic still collapsed: 90d = 50 clicks / 336 impr, last 28d = 6 clicks / 153 impr.
  Winner page `/blog/how-to-get-free-samples-freebies-india` at pos 14.2 (was ~6.9).
- Blog cadence: the 06:09 cron tick is this one and it landed. Day is at 2/3.
  The 12:09 tick owes the third. If it does not land, the post gets written inline.
- No new LIVE deal since 04:49 IST. The `23 */2 * * *` ingest tick at 06:23 is
  most likely queued behind this blog tick (session crons run serially), not dead
  — CronList shows it alive. Re-check after 08:23.
- DB pool cap in `apps/api/.env` still unshipped — owner call.

## 12:4x IST tick

- Today IST count was 2 (target 2-3, cap 4) -> published 1. Day now at **3**. Target met.
- Post: `open-box-delivery-amazon-flipkart-india-2026` (id 372, 8,368 chars / 1,383 words).
  seoTitle 52 chars, seoDesc 153, 4 tags (amazon, flipkart, shopping-guide, returns).
  Internal links -> `/offers` + live deal `/punnkfunnk-novolux-hd-display-smartwatch-...-b0dt9h` (200) +
  `/blog/amazon-flipkart-return-refund-policy-india-2026` (200) +
  `/blog/extended-warranty-appliances-worth-it-india-2026` (200).
- IndexNow: **HTTP 200** via `insert-blog-mdmeta.mjs`. No 422, Bing fallback not needed.
- Cover: `https://richdeals.sfo3.digitaloceanspaces.com/og/open-box-delivery-amazon-flipkart-india-2026.png` (HTTP 200).
  Alt confirmed in rendered HTML = post title. Page live HTTP 200.

### Keyword basis

GSC `sc-domain:richdeals.in` 2026-08-20 -> 2026-09-16, by query, top 50: **2 clicks / 67 impressions total.**
Striking distance is thin and mostly already covered (giordano watch price pos 9.0, goboult ear buds pos 3.0,
amazon pay later cibil score pos 51 -- that one shipped in the 00:4x tick). The whole freebies cluster sits
pos 83-97 with 0 clicks, so the free-samples cluster was **deliberately avoided**: it is decayed and
saturated (46 of 313 slugs), and another post there is padding, not traffic.

Slug-gap scan over all 311 pre-existing slugs found **zero** coverage of open-box / delivery-mechanic topics
(`grep -nE "box|unbox|delivery|open"` returned 3 hits, all lunch-box product roundups). Clean gap, evergreen,
India-specific, no near-dup.

**Fact-handling note:** every claim is sourced. Amazon side from help nodes `GNHMSYW5BTFBTMEW` and
`TpCYK1p37flRka6abZ` (eligible categories, pre-paid only, 6-digit OTP to registered email + SMS, and the
explicit statement that after the code is shared returns for damage / missing parts / wrong item are not
accepted). Flipkart side from `stories.flipkart.com/open-box-delivery-flipkart-customer-trust` (free,
Wishmaster opens in customer presence, mobiles/laptops/large appliances, select locations, eKart) plus the
card-on-delivery exclusion and immediate-refund-on-refusal behaviour. No stats invented; the Flipkart article
publishes none and the post quotes none.

### CEO audit

| check | result |
|---|---|
| posts today IST | **3** (370, 371, 372) -- target met |
| posts/day IST last 4 | 09-15:3 09-16:3 09-17:3 09-18:3 |
| published total | 313, **coverless 0, seoless 0** |
| deals PENDING_REVIEW | 0 |
| LIVE deals null price / null image | 0 / 0 |
| broadcast cursor vs DB max | `lastId:10431` = max LIVE id 10431 -- caught up |
| unpushed commits | none |

### ROT FLAGGED (carried, still open)

- **Deal ingest stalled.** LIVE count 10086 and max LIVE id 10431 are both unchanged since the 11:4x
  SITEMON tick. The `23 */2 * * *` indiafreestuff tick produced zero new deals this window. Cursor is
  caught up, so this is a source/ingest problem, not a broadcast problem. Watch the next 14:23 run;
  if it is also zero, the sweep is dead and needs a hand-run.
- **`.claude/agents/deal-ingest.md` is stale on two points** -- still says `"status": "pending-review"`
  (AUTO-APPROVE killed the review gate) and still names the EarnKaro flow for Flipkart (the matrix is
  `affid=djhackraj` / Cuelinks for everything non-Amazon). Doc will mislead the next ingest run.
- **Amazon extraction fixes still not baked into `apps/api/scripts/lib/ingest-common.mjs`** -- buybox-first
  read, CSS-polluted-buybox fallback, MRP via `pay/(1-savingsPercentage)`, `#add-to-cart-button` as the
  in-stock signal. Re-derived by hand every single tick. This is the highest-value unshipped code fix.
- **`where to get free samples`: 11,072 impressions, pos 6.8, 0 clicks.** Still the biggest single signal on
  the property and still unaddressed. 46 of 313 slugs are in that cluster. Needs merge/prune, not posts.
- Organic still collapsed: last-28d pull this tick shows 2 clicks / 67 impressions.
- 307 LIVE deals carrying pointless `updatedAt` re-stamps -- undiagnosed.
- W6 (`/coupons` + `/freebies` ignore `?type=`) and W8 (`sku` from ASIN needs `productId` on the shared DTO)
  both still need an API change.
- ~605 untracked scratch files under `apps/api/` (git porcelain ~1589 lines).

### Owner decisions still open

1. Ratify publish-at-live-price + the 30% minimum live-discount floor in CLAUDE.md.
2. Permanent DB pool cap in `apps/api/.env`.
3. DesiDime Task Scheduler job `7,37 * * * *` (external cron, needs explicit go-ahead).
4. Free-samples cluster consolidation (46 of 313 slugs).
5. Scratch-file cleanup under `apps/api/`.

## 18:0x IST tick (CONTENT-SEO)

**No post published — gate hit.** Today IST count = **3** (370 amazon-pay-later-cibil-score,
371 flipkart-supercoins, 372 open-box-delivery). Target 2-3/day met, cap 4 not needed.
The tick rule is "skip if >=3", so this tick is a no-op by design, not a miss.

Posts/day IST last 5: 09-14:4 09-15:3 09-16:3 09-17:3 **09-18:3**. Floor never breached.

### CEO audit

| check | result |
|---|---|
| posts today IST | 3 — target met, skip correct |
| published total | 313, coverless 0, seoTitle-less 0, seoDesc-less 0 |
| deals LIVE / EXPIRED | 10155 / 257 |
| LIVE null price / null image | 0 / 0 — nothing to classify or delist |
| PENDING_REVIEW | 0 |
| prod `/api/deals` head id | 10500 = DB max LIVE id |
| prod 7 endpoints (`/` `/offers` `/blog` `/sitemap.xml` `/feed.xml` `/api/deals` `/llms.txt`) | 200 x 7, 99-418ms |
| broadcast cursor | `lastId:10482` vs max 10500, 18 behind — external cron, self-heals |
| unpushed commits | none (`87fdea9` = origin/master) |

### Why no fourth post

The cap is 4 but the target is 2-3. A fourth post today would land in the same two
saturated clusters the last three ticks already avoided (free-samples: 46 of 313 slugs;
best-X-under-Y). `dup-slug-thin-content` says stop padding the count. Writing a fourth
to fill a quota is the exact failure the merge/prune backlog exists to undo.

### ROT FLAGGED (carried, unchanged)

- **`where to get free samples`: 11,072 impressions / pos 6.8 / 0 clicks.** Still the
  single biggest signal on the property, still unaddressed. 46 of 313 slugs sit in that
  cluster. Needs merge/prune, not posts. Highest-value unshipped SEO action.
- Organic collapsed: last-28d GSC = 2 clicks / 67 impressions.
- `apps/api/scripts/lib/ingest-common.mjs` Amazon extractor unshipped — 10 ticks.
- `curlFinal` cannot follow client-side redirects; body-grep fallback proven twice,
  still not in the shared lib.
- `.claude/agents/deal-ingest.md` stale on 4 points.
- W6 (`/coupons` + `/freebies` ignore `?type=`) and W8 (`sku` from ASIN needs `productId`
  on the shared DTO) both need an API change.
- ~605 untracked scratch files under `apps/api/`.

### Owner decisions still open

1. Ratify publish-at-live-price + the 30% minimum live-discount floor in CLAUDE.md.
2. Permanent DB pool cap in `apps/api/.env`.
3. DesiDime Task Scheduler job `7,37 * * * *`.
4. Free-samples cluster consolidation (46 of 313 slugs).
5. Scratch-file cleanup under `apps/api/`.
