# SITEMON + CEO audit tick — 2026-09-19e (IST)

7/7 prod endpoints 200, prod newest deal matches the DB max exactly, CEO audit clean on every
item. **Nothing fixed inline — nothing safe was broken.** Recorded explicitly, because "fixed
nothing" and "did not look" read identically in a tick line otherwise.

This tick is the first read after the `telegram-2026-09-19L` repairs, so its job is partly to
confirm those two writes landed the way they were reported. Both did.

## Uptime / status

```
200 0.219188  /
200 0.130391  /offers
200 0.506252  /blog
200 0.313157  /sitemap.xml
200 0.108082  /feed.xml
200 0.131600  /api/deals
200 0.340633  /llms.txt
```

Every sample inside its normal band. `/` at 0.219 s is the fastest reading in three ticks,
which retroactively confirms the 1.864 s outlier at 09-19d was the cold start it was closed as
and not the leading edge of something.

## Live deal count sanity — prod vs DB

```
prod /api/deals?limit=1 → id 10651  ₹199
  slug vanelis-vanilla-musk-eau-de-parfum-50ml-b0h86r93nv
DB   max deal id        → 10651 LIVE ₹199  (same slug)
```

Agreed on id, slug and price. No serving lag.

**The sitemap has caught up.** `<loc>` count **9604**, up from 9584 at 09-19d — **+20 exactly**,
which is the ingest batch that was still inside its ISR window last tick. It landed on its own,
no manual regen, as predicted.

That +20 is exact, and the exactness says something the count alone does not:

```
grep -c 'milk-peda-sweets-150-gm-137' prod /sitemap.xml → 0
```

Deal **#1609 was delisted to EXPIRED last tick, yet the sitemap total rose by the full 20** with
no offsetting −1. So that row **was never in the sitemap to begin with** — with `mrp` null it
had no discount to clear `dealIndexable`'s ≥20% arm, and it failed the ≥200-char arm too. The
delist therefore bought no index hygiene; what it bought was a page that no longer quotes ₹90
for a ₹137 product. Worth stating plainly rather than claiming a sitemap win the numbers do not
support.

**Both repairs verified in the DB, not assumed:**

```
#2569  LIVE     ₹3540 / ₹6290  -44%
#1609  EXPIRED  ₹137  / ₹150   -9%
```

Exactly what the Telegram tick reported writing.

**Ingest pace:** 222 deals in 24 h, **949 in 7 d ≈ 136/day** — identical to 09-19d, and within
one deal/day of 09-19c and 09-19b. Four consecutive readings in a two-deal band; throughput is
steady.

Newest deal was created 11:13 IST, ~50 minutes ago. Inside the every-2-hours ingest cadence, so
no gap to explain.

## CEO audit (verified against the DB)

| Check | Result |
|---|---|
| Deals | LIVE **10304** · PENDING_REVIEW **0** · EXPIRED **259** |
| LIVE null price / null image | **0 / 0** |
| DB max deal | **10651** LIVE ₹199 — matches prod exactly |
| Posts/day IST (7d) | 09-13:4 · 09-14:4 · 09-15:3 · 09-16:3 · 09-17:3 · 09-18:3 · 09-19:2 |
| Today (IST) | 2 — inside the 2-3 target, under the cap of 4, no zero day in the window |
| Blog hygiene | published 315 · noCover 0 · noSeoTitle 0 · noSeoDesc 0 |
| tg-broadcast cursor | 10482 vs 10651 — **drift 169**, unchanged (rot #4) |
| Unpushed commits | 0 |

**Nothing moved since the last tick, and that is the finding.** LIVE, EXPIRED, max id, post
counts and the cursor are all byte-identical to the 09-19L reading an hour ago. No ingest ran in
the window, so there was nothing to reconcile — the counters holding still is the correct
result, not a stale query. Confirmed live by the `createdAt` timestamp above rather than
inferred from the counts agreeing.

PENDING_REVIEW is 0 **by absence** from `deal.groupBy({by:['status']})`, not by a zero row — an
absent key and a broken query look identical in that output.

The null-price/null-image arm is a no-op for the **seventh** consecutive tick. The
classify-then-delist path had nothing to act on. Note that last tick's delist came from a
*price-accuracy* finding, not from this arm — a row can be junk with both columns populated,
which is precisely the gap this check does not cover.

Posts today stand at 2 in IST — inside the 2-3 target, bottom of the band, day still running.
The BLOG tick owns closing it.

## Fixed inline

Nothing. The audit set came back clean, prod agrees with the DB on id, slug and price, the
sitemap has caught up to the batch it was lagging, and the two rows repaired last tick verified
as written. No safe rot was available to fix.

## Rot standing — 28 items

**Nothing moved.** #4 stays frozen at drift **169** — the last two ticks pushed no deals, so it
neither grew nor shrank. It only closes when the external tg-broadcast cron runs (owner decision
#3), and at 169 messages an unattended drain would still dump the channel's whole backlog at
once, so that cursor stays untouched without explicit go-ahead.

**#12** stays half-fixed — CLAUDE.md still documents chunked `/sitemap/deals.xml` routes that
prod 404s, while one `/sitemap.xml` serves all 9,604 URLs.

**#22** holds at six stale LIVE rows; two of the six were repaired last tick, which lowers the
open count but not the class — nothing systematic prevents the seventh.

Nothing new rotted.

## Open owner decisions — 10

**#7** (persist a reject cache, rot #19) stays top.

**#6** (periodic re-verify sweep over old LIVE rows) gained the strongest supporting datum yet
this hour, from this tick rather than the last: the sitemap arithmetic shows #1609 had been
sitting LIVE and **invisible to search** since July — wrong price, null MRP, excluded from the
sitemap, and nothing in the system would ever have looked at it again. A sweep is the only thing
that finds rows in that state, because no ingest, no audit arm and no crawler touches them.

**#9** (confirm a 0%-discount exact-price match never publishes) is answered by precedent and
awaiting the owner's word to close.
