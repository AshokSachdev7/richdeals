# SITEMON + CEO audit — 2026-09-20b (IST)

**7/7 endpoints 200. Every audit arm clean. Nothing fixed inline because nothing safe was broken.**
One movement worth recording: **the tg-broadcast cursor advanced again on its own**, from 10,719 to
10,729, confirming yesterday's downgrade of rot #4 rather than merely repeating it.

## Endpoints

| Path | Status | Time | Size |
|---|---|---|---|
| `/` | **200** | 0.238s | 319 KB |
| `/offers` | **200** | 0.100s | 60 KB |
| `/blog` | **200** | 0.550s | 149 KB |
| `/sitemap.xml` | **200** | 0.295s | 2,111 KB |
| `/feed.xml` | **200** | 0.129s | 49 KB |
| `/api/deals` | **200** | **1.130s** | 42 KB |
| `/llms.txt` | **200** | 0.268s | 15 KB |

### `/api/deals` at 1.13 s was a cold cache, not rot

The 09-20 baseline had it at 0.120 s. A 9x jump tripped the slow-sample rule, so it was re-measured
three times: **0.0947 s / 0.1003 s / 0.1763 s**. All three sit at or below baseline.

The first sample was the cold one. **Not flagged, and deliberately not carried forward as a
finding** — this is exactly the shape of the mistake the 09-20 report caught itself making with
rot #4, where an unremeasured number got repeated three times as if it were evidence. One slow
sample is a sample, not a trend.

## Deal counts

| | 09-20 baseline | now | Δ |
|---|---|---|---|
| LIVE | 10,387 | **10,388** | **+1** |
| PENDING_REVIEW | 0 | **0** | — |
| EXPIRED | 259 | **259** | — |
| Max deal id | 10,734 | **10,735** | +1 |

The +1 is this tick's Telegram deal (10735, satin scrunchies). **No unexplained movement.**

Ingest rate: **47 in 24h, 711 in 7d ≈ 102/day.** Unchanged from the baseline — the 24h window has
not yet rolled past this morning's two DEAL-INGEST batches.

## Audit set — every arm verified against the DB before flagging

| Arm | Result |
|---|---|
| Posts-per-day IST, never 0 | 09-20 = **1**, 09-19 = 3, 09-18 = 3, 09-17 = 3, 09-16 = 3, 09-15 = 3, 09-14 = 4 |
| Posts-per-day cap 4 | never exceeded (09-14 sits exactly at the cap) |
| Coverless posts | **0** of 317 |
| Posts missing seoTitle | **0** |
| Posts missing seoDesc | **0** |
| LIVE deals with null price | **0** |
| LIVE deals with null image | **0** |
| LIVE with price+mrp but null discountPct | **0** |
| PENDING_REVIEW backlog | **0** |
| `at Rs ` in LIVE titles (last 2,000) | **0** |
| tg-broadcast cursor vs DB max | **10,729** vs 10,735 — **drift 6** |
| Unpushed commits | **0** |
| Sitemap `<loc>` count | **9,689** — unchanged |

### Rot #4 confirmed healthy by a second independent reading

The 09-20 report downgraded #4 from "growing, unattended" to "healthy, lags by one batch" after
finding the cursor at 10,719 rather than the 10,482 the 09-19 baseline recorded.

That downgrade now has a **second data point**: the cursor is at **10,729**, ten rows further on,
with no intervention from this session. The external tg-broadcast cron ran between the two ticks and
drained ten of the fifteen rows it was behind. Drift is **6** against a DB max of 10,735, and five
of those six are from the DEAL-INGEST batch plus the deal published minutes ago.

**The cursor self-heals.** One reading proved the cron was alive; two readings prove it keeps up.
Rot #4 should be closed rather than merely downgraded — the only thing it now measures is the gap
between a publish and the next broadcast run, which is the system working as designed.

### Blog: 09-20 still sits at 1 post

The hard rule is 2-3/day, never 0, cap 4. Today has **1**, and the seven days before it ran
4, 3, 3, 3, 3, 3.

**Not fixed inline — deliberately, and for the same reason as the last tick.** CONTENT-SEO is not
one of the two ticks in this request. Publishing a post requires keyword research, a cover image,
and an IndexNow ping through `insert-blog-mdmeta.mjs`. That is a tick of its own, not a safe inline
repair, and faking it with a hand-rolled insert would break the cover and ping rules at once.

Flagged for the second consecutive tick. **3 slots remain under the cap today.**

### Sitemap unchanged at 9,689

Same count as the 09-20 baseline. Expected: `sitemap.xml` is ISR `revalidate = 1800`, this tick's
single new deal landed minutes ago, and the 30-minute window has not turned over. It is not a
staleness bug — the previous tick's +49 proved the window turns.

The 9,689 against 10,388 LIVE is `dealIndexable()` doing its job: EXPIRED, null price, no image,
failing the discount≥20% / description≥200-char arm, or older than 120 days. Today's deal clears it
on both arms (−83%, description well past 200 chars) and will appear on the next revalidation.

`llms.txt` is `force-dynamic` — rebuilt at request time, returned 200 at 15 KB, needs no freshness
check.

## Fixed inline this tick

**Nothing.** Every arm came back clean. The one item below target (blog at 1/day) is not a safe
inline fix, the one endpoint that looked slow was a cold cache, and the one item that has been
carried as long-standing rot (#4) got better on its own for the second reading running.

## Standing items unchanged

- **Rot #19 now has a third instance** — `PWBGGD4THDQZYAY6` in `data/tg-multi-seen.json` with no DB
  row, the same pid flagged earlier today. Needs owner decision #7.
- Rot #12 doc half — CLAUDE.md still documents chunked `/sitemap/deals.xml` routes that 404 on prod.
  Doc-only; the real `sitemap.xml` verified above.
- Deal 7637 (`boldfit-anti-burst-gym-exercise-ball-55cm-with-foot-pump`) still a candidate for
  EXPIRED.
- `ingest-common.mjs` gaps re-evidenced by this hour's Telegram tick: **`link.amazon` path segments
  are not ASINs** (three instances in one sweep), **browser UA on all shortlink resolution** (made
  `fkrt.co` resolve where curl's default 403'd), `bitli.in`/`bittli.in` `dl=` handling, the
  post-resolution `/s?` assert, the Shopsy `finalPrice`/`mrp` fallback (ld+json Product absent
  entirely this time), and the `_SL1500_` core-extraction upgrade.
- The `want` filter still matches "SB Loots And Deals Help Bot".
- Two same-named `ifs-candidates.json` files.
