# SITEMON + CEO audit — 2026-09-20f (IST)

**All 7 endpoints 200. Every audit arm clean. One watch item: sitemap `<loc>` count flat at 9,701 while the DB gained 22 deals this hour — ISR staleness, not loss.**

## Endpoints

| Endpoint | Status | Bytes | Time |
|---|---|---|---|
| `/` | 200 | 306,019 | 0.3194 s |
| `/offers` | 200 | 60,233 | 0.5291 s |
| `/blog` | 200 | 148,710 | 0.5673 s |
| `/sitemap.xml` | 200 | 2,113,476 | 0.3584 s |
| `/feed.xml` | 200 | 46,496 | 0.1331 s |
| `/api/deals` | 200 | 33,550 | 0.1610 s |
| `/llms.txt` | 200 | 14,653 | 0.4286 s |

`/` at **0.3194 s** — fourth consecutive non-reproduction. The 2.016 s flag stays **withdrawn**.

### Watch item: sitemap loc count unchanged

Sitemap carries **9,701 `<loc>`** — byte-identical size and identical count to the previous tick,
even though the DB gained **22 LIVE deals** since that measurement (21 from deal-ingest 0920d, 1 from
telegram 0920g).

That is **expected**, not rot: `sitemap.xml` is ISR `revalidate = 1800`, so worst case is 30 minutes
stale, and both pushes landed inside that window. Recording it explicitly so the next tick has a
baseline — **if 9,701 is still the number next hour, the revalidate is not firing and that IS rot.**
The IndexNow pings for both batches already returned 200, so discovery does not depend on this.

## Deal DB

| Metric | Value |
|---|---|
| LIVE | **10,420** |
| PENDING_REVIEW | **0** |
| EXPIRED | 259 |
| DRAFT | 0 |
| Max deal id | 10,767 |
| Created last 24 h | 80 |
| Created last 7 d | 744 |
| LIVE with null price | **0** |
| LIVE with null image | **0** |

LIVE moved 10,398 → 10,420, exactly **+22**, matching the two pushes this hour with nothing lost and
nothing double-counted. 24 h rate jumped 58 → 80 for the same reason.

Each status counted with its own `count()` — `groupBy({by:['status']})` omits zero-row statuses, which
is precisely how a PENDING_REVIEW backlog would hide.

Null price and null image both **0**. Nothing to classify, nothing to delist to EXPIRED.

## Blog

318 posts. **0 coverless, 0 missing seoTitle, 0 missing seoDesc.**

Per day, bucketed IST (`d.getTime()+19800000`, because Git Bash ignores `TZ`):

| Date (IST) | Posts |
|---|---|
| 09-14 | **4** |
| 09-15 | 3 |
| 09-16 | 3 |
| 09-17 | 3 |
| 09-18 | 3 |
| 09-19 | 3 |
| 09-20 | 2 |

Never 0, never over the cap of 4. 09-20 sits at **2** — inside the 2-3 band, at its floor, with the
day still running. Compliant. No BLOG tick was requested this hour and none was run.

## Broadcast cursor

`.tg-broadcast-cursor.json` → `lastId: 10767`, **equal to the DB max. Gap 0.**

**Fifth self-heal, and the sharpest one yet:** the cursor has already consumed deal 10767, the Milton
bottle published minutes ago in telegram 0920g, plus all 21 from deal-ingest 0920d. Last tick those 21
sat above the cursor and were reported as pending broadcast; they are now through. **Rot #4 stays
CLOSED** — drift is the external cron working, and the standing rule is to re-read the file rather
than carry a stale gap forward.

**Cursor not drained.** That still needs the owner's explicit go-ahead.

## Git

**0 unpushed commits** at measurement time (before this tick's own report commit).

## CEO audit summary

| Arm | Result |
|---|---|
| Prod endpoints (7) | all 200 |
| `/` latency | 0.3194 s — fourth non-reproduction, flag stays withdrawn |
| Sitemap locs | 9,701 — flat vs +22 deals; ISR window, re-check next tick |
| Live deal count sanity | 10,420 LIVE, +22 this hour, +80 in 24 h — arithmetic reconciles exactly |
| Posts/day IST | 4/3/3/3/3/3/2 — never 0, never over 4 |
| Coverless / seo-less posts | 0 / 0 / 0 |
| Deals null price / null image | 0 / 0 |
| PENDING_REVIEW backlog | 0 |
| Cursor vs DB max | gap 0, fifth self-heal, this hour's 22 consumed |
| Unpushed commits | 0 |
| Artifacts | none — terminal + this file only |

### Rot flagged

Nothing new from this tick's own arms. Carried, verified against the DB before flagging:

- **1,602 LIVE deals carry no MRP, 263 from the last 30 days** — largest data gap on the site, the
  concrete target for owner decision #6. Every one has a price; the missing field is `mrp` only. The
  old "null-discount arm blind spot" framing was wrong and is **not** repeated.
- **LIVE deal 10031** carries `productId` `ae27f94b3330`, a hex hash rather than an ASIN — raised in
  telegram 0920g. Store still unchecked, so still an observation, not a finding.
- Deal 7637 — EXPIRED candidate, still carried.
- Deal 1536 — slug says ₹299, row says ₹499.
- ~200 scratch leftovers under `apps/api/scripts/` (owner decision #5).
- Doc rot: CLAUDE.md still says "RSS first" while feedburner returns HTTP 000, and still claims
  plain-curl ld+json for non-Amazon while Flipkart PDPs 403 with reCAPTCHA.
- Telegram channel quality: `fkrt.co/l5KOxl` Syska at its **seventh** repost, Shopsy bare-number at
  its **fifth** — both feed owner decision #8.

## Fixed inline

**Nothing.** Every arm came back clean. An empty fix list on a green tick is the correct output —
inventing work here would only add risk to a healthy system.
