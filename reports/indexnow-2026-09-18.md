# INDEXNOW tick — 2026-09-18

Key `33f3a9d63ca15676bbd90586ea80e65f`, via `apps/api/scripts/indexnow-ping.mjs`.

## Submitted

| set | count |
|---|---|
| LIVE deals created in last 6h | 63 |
| posts published in last 6h | 1 (`open-box-delivery-amazon-flipkart-india-2026`) |
| `sitemap.xml` | 1 |
| hubs auto-appended by the script (`/`, `/offers`) | 2 |
| **total submitted** | **67** |

**Result: `DONE: IndexNow -> HTTP 200 for 67 urls`.**
No 422 from `api.indexnow.org`, so the Bing GET fallback was not needed and
was not invoked.

## Deliberate exclusion

The 6h window also matched **148 LIVE deals whose `updatedAt` moved but whose
`createdAt` did not** — the known pointless-re-stamp rot. Those rows carry no
content change, so resubmitting them is pure noise to IndexNow and burns
quota against genuinely new URLs. Submitted `createdAt`-in-window only.

Raw 6h counts for the record: 211 deals touched, 63 actually new, 148
re-stamp-only.

## CEO audit

| check | result |
|---|---|
| LIVE deals | 10,110 (max LIVE id 10455) |
| PENDING_REVIEW | 0 |
| LIVE null price / null image | 0 / 0 |
| posts published total | 313 — coverless 0, seoless 0 |
| posts today IST | 3 (target met, cap 4) |
| prod `/` `/sitemap.xml` `/feed.xml` `/llms.txt` | 200 × 4 |
| unpushed commits | 0 |
| tg-broadcast cursor | `lastId:10442` vs max LIVE 10455 — 13 behind, moving (was 10437 last tick) |

### ROT FLAGGED (carried)

- **148 LIVE deals re-stamping `updatedAt` with no content change** — was
  logged as "307 deals" and undiagnosed; this tick pins the live rate at ~148
  per 6h window. It makes every freshness surface lie and forces every
  IndexNow tick to filter by `createdAt` by hand. Worth a real diagnosis: find
  what writes `updatedAt` on unchanged rows (broadcast flip, re-verify loop,
  or a bulk upsert that always writes).
- tg-broadcast cursor still 13 behind but advancing — external cron alive.
- `.claude/agents/deal-ingest.md` stale (pending-review status, EarnKaro for
  Flipkart *and* Other stores, `data/deals/index.json` dedup, placeholder
  amazonTag).
- Amazon extraction fixes still not baked into `apps/api/scripts/lib/ingest-common.mjs`
  (+ shopsy `finalPrice` fallback). Highest-value unshipped code fix.
- tg seen-cache stores "processed", not "published" — transient skips never retried.
- `where to get free samples` 11,072 impr / pos 6.8 / 0 clicks; 46 of 313 slugs
  in that cluster. Merge/prune, not more posts.
- Organic last-28d: 2 clicks / 67 impressions.
- W6 (`/coupons` + `/freebies` ignore `?type=`), W8 (`sku` from ASIN needs
  `productId` on the shared DTO) — both need an API change.
- ~605 untracked scratch files under `apps/api/`.

### Owner decisions still open

1. Ratify publish-at-live-price + 30% minimum live-discount floor in CLAUDE.md.
2. Permanent DB pool cap in `apps/api/.env`.
3. DesiDime Task Scheduler job `7,37 * * * *`.
4. Free-samples cluster consolidation (46 of 313 slugs).
5. Scratch-file cleanup under `apps/api/`.
