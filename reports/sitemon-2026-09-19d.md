# SITEMON + CEO audit tick — 2026-09-19d (IST)

7/7 prod endpoints 200, prod newest deal matches the DB max exactly, CEO audit clean on every
item. **Nothing fixed inline — nothing safe was broken.** Recorded explicitly, because "fixed
nothing" and "did not look" read identically in a tick line otherwise.

This tick ran immediately after the `deal-ingest-2026-09-19f` batch of 20, so most of the
numbers below moved, and each move is accounted for rather than reported as a new level.

## Uptime / status

```
200 1.863990  /
200 0.123313  /offers
200 0.439814  /blog
200 0.519607  /sitemap.xml
200 0.096856  /feed.xml
200 0.146709  /api/deals
200 0.418840  /llms.txt
```

`/` at **1.864 s** is ~6x its normal band (0.25-0.37 s), so it was re-measured rather than
either ignored or logged as rot:

```
200 0.313827
200 0.171865
200 0.181876
```

Three consecutive samples inside or below the normal band. **Closed as a cold-start one-off,
not opened as rot** — same disposition the `/blog` 1.20 s sample got at 09-19h, which then
failed to repeat three times and was closed at 09-19c. The rule being applied consistently:
a single slow sample is re-measured in the same tick; it becomes rot only if it repeats.

Everything else is inside its normal band.

## Live deal count sanity — prod vs DB

```
prod /api/deals?limit=1 → id 10651  ₹199
  slug vanelis-vanilla-musk-eau-de-parfum-50ml-b0h86r93nv
DB   max deal id        → 10651 LIVE ₹199  (same slug)
```

Agreed on id, slug and price. No serving lag. That row is the **last row of the ingest batch
pushed minutes ago**, so the write-to-serve path is confirmed end to end on a deal this audit
watched land — the strongest form of this check, not just a matching pair of numbers.

**The sitemap does NOT carry the batch yet, and that is reported rather than glossed.** Prod
`/sitemap.xml` `<loc>` count is **9584** (was 9583 at 09-19c), and `grep` for three of the 20
new slugs (`hikvision-k5-2k`, `larah-by-borosil-27-piece`, `vanelis-vanilla-musk`) returns
**0**. The sitemap is ISR `revalidate = 1800`; the push happened inside the current window, so
the batch lands within 30 minutes on its own. No manual regen, nothing to fix. The +1 from 9583
is the Telegram tick's #10631, which was pushed early enough to make the last turnover.

`llms.txt` is `force-dynamic` and already carries the batch, so the AI-facing surface is not
waiting on ISR.

**Ingest pace:** 222 deals created in the last 24 h, **949 in 7 days ≈ 136/day** — flat against
the 137/day at 09-19c and the 136/day at 09-19b. Three consecutive readings within one deal per
day of each other; the pipeline's throughput is steady, not drifting.

## CEO audit (verified against the DB)

| Check | Result |
|---|---|
| Deals | LIVE **10305** (was 10284) · PENDING_REVIEW **0** · EXPIRED 258 |
| LIVE null price / null image | **0 / 0** — 20 new rows kept it that way |
| DB max deal | **10651** LIVE ₹199 — matches prod exactly |
| Posts/day IST (7d) | 09-13:4 · 09-14:4 · 09-15:3 · 09-16:3 · 09-17:3 · 09-18:3 · 09-19:2 |
| Today (IST) | 2 — inside the 2-3 target, under the cap of 4, no zero day in the window |
| Blog hygiene | published 315 · noCover 0 · noSeoTitle 0 · noSeoDesc 0 |
| tg-broadcast cursor | 10482 vs DB max 10651 — **drift 169**, up from 148 (rot #4) |
| Unpushed commits | 0 before this tick |

**The +21 is arithmetic, not an anomaly.** LIVE went 10284 → 10305, which is one more than the
20 rows the ingest batch pushed. The extra row is #10631 from the Telegram tick, pushed after
the 10284 reading was taken at 09-19c. Stated because a +21 sitting next to a batch of 20 is
exactly the shape a double-insert bug would make, and "close enough" is how that gets missed.

PENDING_REVIEW is 0 **by absence** from `deal.groupBy({by:['status']})`, not by a zero row — an
absent key and a broken query look identical in that output.

The null-price/null-image arm of the brief is a no-op for the **fifth** consecutive tick: the
classify-then-delist path has nothing to act on, and 20 fresh rows did not introduce a single
null. That is the verifier in the push script doing its job, not luck.

Posts today stand at 2 in IST. Inside the 2-3 target, so no blog rot — but it is the bottom of
the band with the day still running, and the BLOG tick owns closing it.

## Fixed inline

Nothing. The audit set came back clean, prod agrees with the DB on id, slug and price, and the
only lagging surface (the sitemap) is lagging by design inside its own ISR window. No safe rot
was available to fix.

## Rot standing — 28 items

**#4** is the one that moved, and it moved hard: cursor drift 148 → **169**, grown by exactly
this hour's 21 pushed deals. Every tick that pushes widens it by that push, and it will not
close until the external tg-broadcast cron runs again (owner decision #3). At 169 messages, a
single unattended drain would now dump the channel's entire backlog at once — which is why that
cursor stays untouched without the owner's explicit go-ahead.

**#12** stays half-fixed — CLAUDE.md still documents chunked `/sitemap/deals.xml` routes that
prod 404s, while one `/sitemap.xml` serves all 9,584 URLs.

**#22** gained three more confirmed stale LIVE rows this hour via the ingest tick (#2569,
#6930, #6387), one of which was independently caught by two pipelines within the same hour.
Five stale rows now on record.

Nothing new rotted.

## Open owner decisions — unchanged at 10

**#7** (persist a reject cache, rot #19) stays top. **#3** (recreate or retire the external
crons) is the one with a number attached that grows every hour — **169** now, and it grew by 21
in a single hour rather than the usual 1-6.

**#6** (periodic re-verify sweep over old LIVE rows) climbed on this hour's evidence: five
stale rows on record, one of them caught twice by two pipelines that do not share state.
