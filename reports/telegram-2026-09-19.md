# TELEGRAM-DEAL-MONITOR tick — 2026-09-19 (IST)

## Sweep

Playwright MCP profile `richDeals` → `web.telegram.org/a/`. One `browser_evaluate`
over `.chat-list .ListItem.Chat` → **27 sidebar rows**, covering all 13 source groups in
`data/tg-groups.json`. Seen-cache at tick start: **1,727** ids.

The "Telegram" service row again carried a live login code in its preview. Skipped as a
non-source chat; the value is not recorded here or anywhere else.

## Triage — 13 source groups

| Group | Newest post | Verdict |
|---|---|---|
| CoolzTricks | Fort Collins winterwear, 4 `amzn.to` links | SKIP — multi-product |
| Dealzone | "Upto 70% Off On U.S Polo Assn. Shoes" | SKIP — brand roundup |
| Dealdost | Wall Hook (Pack of 30) @ ₹121, `bitli.in/YJLzp2n` | CANDIDATE |
| SB Loots And Deals | Havells 500W mixer grinder @ ₹1,944 + ₹590 coupon, `amazn.lt/BembWbDh` | CANDIDATE |
| ONLINE SHOPPING DEALS | Aqueria sunscreen SPF 50, preview truncated, no link | SKIP — no URL in preview |
| Rogerkart Deals | Gang's laptop bag 19L @ ₹759, `rogerkart.com/r/pVQu7jR` | SKIP — rot #5, shortlink unresolvable |
| Deal Dibba | "Loot : boAt smartwatches from ₹999", 3 links | SKIP — loot/multi |
| IndiaFreeStuff Tips | Swiggy Instamart search URL | SKIP — search page |
| Hidden Loot Deals | Blinkit freebie, pincode-locked | SKIP — not a product |
| INDIAN CHEAP DEALS | Ladies handbag @ ₹3,500, `link.amazon/B05yvriRF` | CANDIDATE |
| OMG LOOTDEALS | "Video dekho paisa kamao" | SKIP — app promo |
| Loot Deals 24x7 | Flipkart Syska 10000 mAh @ ₹799, `fkrt.co/l5KOxl` | SKIP — same deal rejected 09-18 (OOS, live ₹1,393) |
| NonStopDeals | "151" + `amzn.to/4uZXfjK` | CANDIDATE |

## Shortlink resolve

| Shortlink | Resolves to | Note |
|---|---|---|
| `bitli.in/YJLzp2n` | `meesho.com/s/p/hlnv0j` (via linkredirect) | single product |
| `amazn.lt/BembWbDh` | `amazon.in/dp/B0D7VF87F4` | single product |
| `link.amazon/B05yvriRF` | `amazon.in/dp/B0G38DGNKM` | single product |
| `amzn.to/4uZXfjK` | `amazon.in/dp/B07QX21WZQ` | single product |
| `rogerkart.com/r/pVQu7jR` | *(no redirect — returns itself)* | rot #5 confirmed again |

No resolved URL was a `/s?` search or category page this tick.

## Dedup — seen cache + live DB

| Id | Cache | DB |
|---|---|---|
| `B0D7VF87F4` | SEEN | LIVE id 10541 `havells-capture-500w-mixer-grinder-…` ₹2,534 |
| `B0G38DGNKM` | SEEN | LIVE id 7110 `lavie-luxe-quaro26-…` ₹3,653 |
| `B07QX21WZQ` | SEEN | LIVE id 5825 `trustbasket-uv-treated-plastic-round-pot-…` ₹549 |
| `hlnv0j` | new | NOT IN DB |

Cache and DB agreed on all four this tick.

## Verification

**Meesho wall hook (`hlnv0j`) — REJECTED, unverifiable.** Meesho returns **HTTP 403** to
curl with a browser UA *and* to the logged-in Playwright Chrome (`Access Denied`, no ld+json,
no `__NEXT_DATA__`). No price could be read, so nothing was published. Logged as new rot.

**Three Amazon ASINs re-verified** in the logged-in tab (same-origin fetch) because the source
prices disagreed with what the DB carries:

| ASIN | Source claim | Live PDP | DB | Result |
|---|---|---|---|---|
| `B07QX21WZQ` | ₹151 | **₹549** (MRP 1,009, -45%, in stock) | ₹549 | DB correct; the ₹151 post is bait |
| `B0D7VF87F4` | ₹1,944 | **₹2,534** (MRP 3,995, -37%, in stock) + `Apply ₹590 coupon` | ₹2,534 | DB correct; 2,534 − 590 = 1,944, so the source quotes the coupon-inclusive price |
| `B0G38DGNKM` | ₹3,500 | **₹3,459** (MRP 6,299, -45%, in stock) | ₹3,653 | **DB stale by ₹194 — fixed** |

## Pushed

**0 new deals.** Every Amazon candidate was already live; the only new product
(Meesho wall hook) could not have its price verified.

## Fixed inline (CEO mode)

Deal **id 7110** `lavie-luxe-quaro26-horizontal-small-satchel-handbag-women`:
price `3653 → 3459`, discountPct `42 → 45`, mrp 6,299 unchanged. That page had been
quoting a price ₹194 above the real one.

`node apps/api/scripts/indexnow-ping.mjs lavie-luxe-quaro26-horizontal-small-satchel-handbag-women`
→ **`DONE: IndexNow -> HTTP 200 for 3 urls`** (slug + `/` + `/offers`). No 422, Bing fallback
not needed.

Seen cache 1,727 → **1,728** (`hlnv0j` added so the 403 host is not re-fetched every tick).

## CEO audit (verified against the DB this tick)

- Deals LIVE **10,229**, EXPIRED 257, PENDING_REVIEW **0**, null price **0**, null image **0**.
- Posts **314**, coverless **0**, seo-less **0**.
- Posts-per-day IST: 09-15:3 09-16:3 09-17:3 09-18:3 **09-19:1**. Never 0, never over 4 —
  but today is at 1, so **2 more are due**; the next BLOG tick must not skip.
- **tg-broadcast cursor 10,482 vs max deal id 10,574 = 92 behind.** External cron still dead.
  Parked — draining fires ~92 channel messages at once, owner go-ahead required.
- Unpushed commits before this tick: 0.

## Rot list — 18 items (1 new)

1. `apps/api/scripts/lib/ingest-common.mjs` still has no Amazon extractor — 21st tick hand-derived.
2. Source-vs-live price drift is the dominant reject reason across every source.
3. indiafreestuff Flipkart links structurally dead.
4. tg-broadcast external cron not firing — cursor drift 92.
5. `curlFinal` cannot follow `rogerkart.com/r/…` — confirmed again, the shortlink returns itself.
6. ~~`amazn.lt` NXDOMAIN~~ — corrected, resolves fine.
7. `.claude/agents/deal-ingest.md` stale on 4 points plus RSS.
8. `where to get free samples`: 11,072 impressions / pos 6.8 / 0 clicks; 46 of 314 slugs.
9. Organic collapse: last-28d GSC = 2 clicks / 67 impressions.
10. W6 (`/coupons` + `/freebies` ignore `?type=`) and W8 (`sku` needs `productId` on the DTO) need API changes.
11. ~605 untracked scratch files under `apps/api/`.
12. CLAUDE.md documents chunked sitemaps that prod 404s.
13. `link.amazon` shortlinks can expand to `/s?hidden-keywords=` — reject must run post-resolve.
14. Flipkart PDPs serve no ld+json to a real browser either, and a stale tab lies.
15. tg seen-cache and DB can disagree; only the DB check is authority.
16. The Amazon ₹-coupon badge is not extracted by any shared code — hand-read again this tick,
    and it is exactly what explains the SB Loots ₹1,944 "drift".
17. indiafreestuff Feedburner RSS dead (HTTP 000).
18. **NEW — Meesho is unverifiable.** `meesho.com/s/p/…` returns 403 to curl *and* to the
    logged-in Playwright browser. Every Meesho deal will be rejected on price-verify until a
    working read path exists. `bitli.in` (linkredirect) shortlinks mostly land on Meesho, so
    this silently kills a whole source channel.

## Open owner decisions (5, unchanged)

1. Ratify publish-at-verified-live-price + the 30% floor in CLAUDE.md, and state that
   coupon-inclusive source prices are reconciled, not rejected (rot #16 made that concrete today).
2. Permanent DB pool cap in `apps/api/.env`.
3. External crons — DesiDime `7,37 * * * *` and tg-broadcast — recreate or retire.
4. Free-samples cluster consolidation (46 of 314 slugs).
5. Scratch-file cleanup under `apps/api/`.
