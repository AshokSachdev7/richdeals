# SITEMON + CEO audit tick — richdeals.in — 2026-09-21

## Uptime

| endpoint | status | time | bytes |
|---|---|---:|---:|
| `/` | 200 | 0.311 s | 309,771 |
| `/offers` | 200 | 0.132 s | 60,233 |
| `/blog` | 200 | 0.404 s | 149,049 |
| `/sitemap.xml` | 200 | 0.296 s | 2,122,586 |
| `/feed.xml` | 200 | 0.131 s | 47,476 |
| `/api/deals` | 200 | 0.137 s | 38,831 |
| `/llms.txt` | 200 | 0.275 s | 14,693 |

All seven green. `/` at **0.311 s** — seventh consecutive non-reproduction of the withdrawn
2.016 s latency flag. That finding stays withdrawn.

## Deal count sanity

**Sitemap locs 9,739 → 9,747, exactly +8** — the eight deals the DEAL-INGEST tick pushed an hour
ago, all carried into the sitemap without a manual regeneration. `/api/deals` returns id **10798**
(`acer-15-smartchoice-amd-ryzen-3-7320u-laptop-b0h6m4`) as the newest row, which is the last of
that batch. Push → DB → sitemap → API chain verified end to end.

| metric | value |
|---|---:|
| LIVE deals | 10,451 |
| EXPIRED deals | 259 |
| PENDING_REVIEW | **0** |
| DB max deal id | 10,798 |
| LIVE with null price | **0** |
| LIVE with null image | **0** |

`groupBy` omits zero-row statuses, so PENDING_REVIEW's absence is the 0 — the auto-approve rule is
holding, nothing is sitting in a review queue.

## CEO audit

**Posts per day, IST:**

| 09-13 | 09-14 | 09-15 | 09-16 | 09-17 | 09-18 | 09-19 | 09-20 | 09-21 |
|------:|------:|------:|------:|------:|------:|------:|------:|------:|
| 4 | 4 | 3 | 3 | 3 | 3 | 3 | 2 | **1** |

Never 0, never over 4 — the hard rule holds. **09-21 sits at 1 with most of the IST day still
open**; the next two CONTENT-SEO ticks need to carry it to 3. 09-20 closed at 2, a soft miss, and
two consecutive short days would turn a soft flag into a real one. **319 posts, 0 coverless,
0 seo-less.**

**Broadcast cursor: `lastId` 10795 vs DB max 10798 — 3 behind, not rot.** The external
tg-broadcast cron is mid-catch-up on the eight deals just pushed; this gap self-heals every run and
must not be carried forward as a finding.

**Nothing unpushed** — `origin/master..master` is empty. Both of today's report commits
(`89bbc4e` telegram, `d9c5c50` deal-ingest) are on the remote.

**Null-data rot: clean.** Zero LIVE rows with a null price, zero with a null image. The earlier
delist-junk-to-EXPIRED sweep is still holding at 259 EXPIRED.

**Nothing safe to fix inline this tick** — every audited surface came back green, so no CEO-mode
repair was warranted. Flagging that explicitly: a quiet tick is a result, not a skipped one.

**Carried rot, unchanged:**

- The **flat-₹ coupon detector gap** raised an hour ago is the live open item — the `%`-only regex
  silently misses `[Apply ₹1500 Coupon]`. It needs a `₹` arm in `ingest-common.mjs` before the next
  ingest tick. Highest-value carried item right now because it under-reports real savings without
  erroring.
- **1,603 LIVE deals with no MRP** (owner decision #6) — up one from the Dyson row published today.
- **#48** Rogerkart `/r/<code>` is a client-side JS redirect. **#47** ONLINE SHOPPING DEALS is
  derivative, 50% dup rate. **#46** CoolzTricks posts nameless coupon claims. **#45**
  `data/tg-multi-seen.json` drifts from the DB both ways. **#44** IFS Flipkart `?rto=` → 403.
  **#43** CLAUDE.md's "homepage HTML fallback" for a homepage with no deal grid.
- Source RSS feedburner HTTP 000 while CLAUDE.md says "RSS first".
- LIVE deal 10031 carries `productId` `ae27f94b3330`, a hex hash.
- ~200 scratch files in `apps/api/scripts/` — owner decision #5.

Scratch hygiene clean — `apps/api/_sm0921.cjs` created and removed in the same Bash call (33rd).
