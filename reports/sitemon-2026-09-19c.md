# SITEMON + CEO audit tick — 2026-09-19c (IST)

7/7 prod endpoints 200, prod newest deal matches the DB max exactly, CEO audit clean on every
item. **Nothing fixed inline — nothing safe was broken.** Recorded explicitly, because "fixed
nothing" and "did not look" read identically in a tick line otherwise.

## Uptime / status

```
200 0.323621  /
200 0.153594  /offers
200 0.508185  /blog
200 0.320156  /sitemap.xml
200 0.093579  /feed.xml
200 0.171304  /api/deals
200 0.304088  /llms.txt
```

`/blog` 0.51 s — inside its normal 0.30-0.60 s band. The 1.20 s sample from 09-19h has now
failed to repeat three times; closing it as noise rather than carrying it further.

## Live deal count sanity — prod vs DB

```
prod /api/deals?limit=1 → id 10630  ₹1066
  slug crompton-laser-ray-neo-20w-led-batten-warm-white-pack-of-8-b09nkbv5xq
DB   max deal id        → 10630 LIVE ₹1066  2026-09-19T04:20:37.644Z  (same slug)
```

Agreed on id, slug and price. No serving lag. That row is the Telegram tick's push from
minutes ago (`139abbe`), so the write-to-serve path is confirmed end to end on a deal the
audit itself watched land.

**Sitemap already carries it** — `grep -c` for the slug in prod `/sitemap.xml` → **1**, total
`<loc>` count **9583** (was 9577 at 09-19b, +6 = the 5 ingest deals plus this one). The ISR
window turned over on its own; no manual regen.

**Ingest pace:** 226 deals created in the last 24 h, 961 in 7 days = **~137/day**, unchanged
from the 136/day measured at 09-19b. Newest LIVE deal is minutes old, against the 2.8 h gap
that tick had to explain — today's sources are producing again.

## CEO audit (verified against the DB)

| Check | Result |
|---|---|
| Deals | LIVE **10284** · PENDING_REVIEW **0** · EXPIRED 258 |
| LIVE null price / null image | **0 / 0** — nothing to classify, nothing to delist |
| DB max deal | 10630 LIVE — matches prod exactly |
| Posts/day IST (7d) | 09-19:2 · 09-18:3 · 09-17:3 · 09-16:3 · 09-15:3 · 09-14:4 · 09-13:4 |
| Today (IST) | 2 — inside the 2-3 target, under the cap of 4, no zero day in the window |
| Blog hygiene | published 315 · noCover 0 · noSeoTitle 0 · noSeoDesc 0 |
| tg-broadcast cursor | 10482 vs DB max 10630 — **drift 148** (rot #4) |
| Unpushed commits | 0 |

PENDING_REVIEW is 0 **by absence** from `deal.groupBy({by:['status']})`, not by a zero row —
an absent key and a broken query look identical in that output.

The null-price/null-image arm of the brief is a no-op again: the classify-then-delist path has
nothing to act on, which is the fourth consecutive tick at 0/0.

## Fixed inline

Nothing. The audit set came back clean, prod agrees with the DB, and the sitemap is current.
No safe rot was available to fix.

## Rot standing — 28 items

**#4** is the only one that moved: cursor drift 147 → **148**, grown by the Crompton batten
this hour. Every tick that pushes a deal widens it by exactly that push, and it will not close
until the external tg-broadcast cron is running again (owner decision #3). **#12** stays
half-fixed — CLAUDE.md still documents chunked `/sitemap/deals.xml` routes that prod 404s
while one `/sitemap.xml` serves all 9,583 URLs.

Nothing new rotted.

## Open owner decisions — unchanged at 10

**#7** (persist a reject cache, rot #19) stays top. **#3** (recreate or retire the external
crons) is the one with a number attached that grows hourly.
