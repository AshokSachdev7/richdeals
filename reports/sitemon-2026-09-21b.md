# SITEMON + CEO audit tick — richdeals.in — 2026-09-21 (b)

## Uptime — seven endpoints, all 200

| endpoint | status | bytes | time |
|---|---:|---:|---:|
| `/` | 200 | 310,839 | 0.244 s |
| `/offers` | 200 | 60,233 | 0.578 s |
| `/blog` | 200 | 149,049 | 0.596 s |
| `/sitemap.xml` | 200 | 2,125,404 | 0.683 s |
| `/feed.xml` | 200 | 49,472 | 0.113 s |
| `/api/deals` | 200 | 40,001 | 0.092 s |
| `/llms.txt` | 200 | 14,693 | 0.374 s |

**7/7 up, nothing degraded.** `/` at 0.244 s is the fastest read in ten ticks — the old `/` latency
flag stays **withdrawn** (tenth consecutive non-reproduction) and is not re-raised here.
`/sitemap.xml` at 0.683 s is a 2.1 MB document, so the time is transfer, not render.

## Live deal count sanity — API agrees with the DB exactly

`/api/deals?limit=1` returns `{"total": 10457, "nextCursor": 10804}`. Prisma reads LIVE = **10,457**
and max deal id = **10,804**. Both numbers match on the nose, so the public API is serving the whole
live set with no filter drift and no stale cache between the DB and the edge.

## Sitemap — the +6 attribution is now complete

Locs **9,761**, bytes **2,125,404** — identical to the previous tick's read, which is correct:
nothing has been pushed since that DEAL-INGEST batch, so there is nothing new to appear.

The previous tick grepped three of the six pushed slugs. The remaining three were grepped this tick
and each returns exactly **one** `<loc>`:

```
digiroot-ipad-pencil-fast-charge-palm-rejection-b0cp91   1
2-in-1-stainless-steel-julienne-vegetable-peeler-b0fpfn  1
amazon-solimo-glass-storage-jar-750ml-set-of-4-b0cf1r    1
```

All **6/6** slugs from ids 10799-10804 are now individually confirmed in the sitemap. The 9,755 →
9,761 delta is fully accounted for by slug, not by arithmetic. That closes the attribution the
`+8` retraction demanded.

## CEO audit — every number re-measured, nothing carried

| metric | value |
|---|---:|
| LIVE deals | **10,457** (= API `total`) |
| EXPIRED | 259 |
| PENDING_REVIEW | **0** (absent from `groupBy` = zero) |
| LIVE with null price | **0** |
| LIVE with null image | **0** |
| LIVE with no MRP | **1,626** |
| DB max deal id | **10,804** (`afast-clear-glass-tea-cup-set-of-6-100ml-b0gv3h`) |
| posts | 319 |
| coverless posts | **0** |
| posts with no seoTitle | **0** |
| posts with no seoDesc | **0** |
| broadcast cursor `lastId` | **10,804 = DB max, caught up** |
| sitemap locs | 9,761 |
| unpushed commits (before this report) | **0** |

**Nothing to fix inline.** The delist-junk-to-EXPIRED authorization in the brief had no work to do:
LIVE null price = 0 and LIVE null image = 0, read fresh, so there is no junk to classify and no row
to delist. Reporting a cleanup here would be inventing one.

**PENDING_REVIEW backlog is zero.** The status `groupBy` returns only `LIVE` and `EXPIRED`; a status
with no rows does not appear, and that absence **is** the zero — not a missing read.

**Broadcast cursor is caught up, re-read this tick.** `.tg-broadcast-cursor.json` reads `lastId`
10804 against DB max 10804. Not carried from the telegram report — `cat`'d fresh.

**Posts per day, IST:** 09-15 `3` · 09-16 `3` · 09-17 `3` · 09-18 `3` · 09-19 `3` · 09-20 `2` ·
09-21 **`1`**. Never 0, never over 4. Node `Date.now()` is `2026-09-20T22:25:50.691Z` = **03:55 IST
on 09-21**, so the current IST day is under four hours old at 1 post — ahead of pace, not behind.
09-20 closing at **2** stays the only real soft miss. Counted in IST, per
`audit-postsperday-ist.md`; a UTC read of this same data would cry rot on a day that has barely
started.

**Clock is not skewed.** node `Date.now()` `22:25:50.691Z` vs PG `now()` `22:25:50.737Z` — 46 ms
apart. Measured, not assumed, before any timestamp finding could be blamed on an offset.

## Rot — nothing new, nothing closed

- **The flat-₹ coupon gap is the top open item and is UNTESTED this tick.** SITEMON publishes
  nothing, so `/(\d+)% ?(?:off )?[Cc]oupon/` in `ingest-common.mjs` was not exercised at all. It
  still cannot see `[Apply ₹1500 Coupon]` and still needs a `₹\s?[\d,]+\s*(?:off\s*)?[Cc]oupon` arm.
  Untested, not fixed, not re-confirmed.
- **1,626 LIVE rows still carry no MRP** — unchanged, and the target of owner decision #6.
- **Two implausible MRPs shipped in the last batch** (₹5,999 on a ₹1,495 stylus, ₹999 on a ₹199
  peeler), read off `.basisPrice` and published as served. The implausible-MRP guard is still
  unwritten.
- **#48** `rogerkart.com/r/<code>` client-side Next.js redirect — `url_effective` never moves.
  **#47** Telegram sources derivative *and* dormant — three straight ticks at zero, eight of
  thirteen rows older than a week. **#46** CoolzTricks nameless coupon claims. **#45**
  `data/tg-multi-seen.json` drifts both ways; the Prisma check is the real dedup. **#44** IFS
  Flipkart `?rto=` → 403, not re-triggered. **#43** CLAUDE.md's "homepage HTML fallback" against a
  homepage with no deal grid, and "RSS first" against a feedburner feed returning HTTP 000.
- `/newsuperdeals` still returns 28 bytes. LIVE deal 10031 still carries `productId` `ae27f94b3330`,
  a hex hash. ~200 scratch files in `apps/api/scripts/` — owner decision #5.

Scratch hygiene clean — `apps/api/_sm0921d.cjs` created and removed in the same Bash call (**45th
clear**). Both sitemap fetches went to temp files outside the repo and were removed in the same
calls.
