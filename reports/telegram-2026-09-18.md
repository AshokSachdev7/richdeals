# TELEGRAM-DEAL-MONITOR — 2026-09-18

## 12:5x IST tick

**Result: 1 deal pushed LIVE, IndexNow HTTP 200.**

### Discovery

One `browser_evaluate` over `.chat-list .ListItem.Chat` on tab 0 (`web.telegram.org/a/`,
Playwright MCP profile richDeals) returned 25 rows covering all 13 groups in
`data/tg-groups.json`. No chat opened, no reload — cheapest read path.

### Triage — skipped as loot / multi / category / search / own channel

| group | post | why skipped |
|---|---|---|
| CoolzTricks Official | Levi's "Upto 76% Off" + two gendered category links | category |
| RichDeals | (our own channel) | self |
| Dealdost | Apple Watch S12 + iPhone 18 + AirPods 5 | multi + pre-order |
| Deal Dibba | boAt / Noise / Fire-Boltt "from ₹999" | multi-brand |
| IndiaFreeStuff Tips | Swiggy Instamart search link | search |
| Hidden Loot Deals | Blinkit freebie | loot |
| OMG LOOTDEALS | "Video dekho paisa kamao" | not a deal |
| Dealzone | Axe perfume "Master Link" → resolved to Amazon `/s?k=` | search page |

### Shortlinks resolved (curl, browser UA, ≥1s apart)

| shortlink | resolved | verdict |
|---|---|---|
| `bittli.in/kpqg7iVu` | shopsy.in `/p/itm2a13ed5cc06d6?pid=XSRH4JZ5GHCHKZYE` | **NEW → pushed** |
| `fkrt.co/l5KOxl` | flipkart.com Syska 10000mAh `pid=PWBGGD4THDQZYAY6` | in seen-cache → skip |
| `amzn.to/4uZXfjK` | amazon.in `/dp/B07QX21WZQ` | seen + LIVE id5825 |
| `link.amazon/B05yvriRF` | amazon.in `/dp/B0G38DGNKM` | seen + LIVE id7110 |
| `link.amazon/B09wy3Vrl` | amazon.in `/s?k=premium+perfume` | search page → drop |
| `rogerkart.com/r/pVQu7jR` | did not redirect (JS-side redirect) | unresolvable by curl |

`link.amazon/<code>` is Amazon's own shortener and resolves to either a PDP or a
search page — it must be checked every time, not assumed to be a `/dp/`.

### Dedup

`data/tg-multi-seen.json` (1,699 entries) + live DB via Prisma:

```
B07QX21WZQ       SEEN  |  LIVE id5825
B0G38DGNKM       SEEN  |  LIVE id7110
PWBGGD4THDQZYAY6 SEEN  |  NOT IN DB
XSRH4JZ5GHCHKZYE new   |  NOT IN DB  -> push
```

### Pushed

- **VRLOOKS Men Self Design Casual Black Shirt** — Shopsy, ₹158 (MRP ₹999, **84% off**)
  - slug `vrlooks-men-self-design-casual-black-shirt-xsrh4j`, deal id **10432**
  - price + stock verified off the live PDP: `finalPrice.value 158`, `mrp 999`,
    `availabilityStatus "IN_STOCK"`, ADD_TO_CART + BUY_NOW present. Message price ₹158, drift ₹0.
  - **ld+json on Shopsy carries no `offers` block** — only `Product{name,image}` +
    `BreadcrumbList`. Price had to come from the embedded `finalPrice` JSON. Note this for
    the next Shopsy deal: `productLd()` alone returns nothing usable on that host.
  - image `rukmini1.flixcart.com/.../s-popcorn-black-vrlooks-original-imahcbpmdzjvbpgn.jpeg`
    (marketplace CDN, HTTP 200, 93,948 bytes). The PDP's first image is a *different* brand
    (`pushya`) — picked the `vrlooks` variant deliberately.
  - Host is `shopsy.in`, **not** `flipkart.com`, so per the affiliate matrix it is an
    "everything else" store → **Cuelinks**, not the `affid=djhackraj` Flipkart rule.
    Source `affid=inf_f90accbd-…` stripped before wrapping.
  - `/admin/deals/bulk` HTTP **201**, `count:1`, `created:true`, `status:live`.
- **IndexNow: HTTP 200** for 3 urls (`indexnow-ping.mjs`, slug + `/` + `/offers` hubs).
- Live page `https://richdeals.in/vrlooks-men-self-design-casual-black-shirt-xsrh4j` → **200**.
- `XSRH4JZ5GHCHKZYE` appended to `data/tg-multi-seen.json` (now 1,700).

### Not recovered

`ONLINE SHOPPING DEALS` posted LENSKART BLU glasses at ₹499 behind
`https://link.amazon/B04NQ7A…` — the sidebar `last-message` is **truncated at the Telegram
source**, so the code is incomplete. Removing the `.slice()` does not help; only a trusted
sidebar click into that chat recovers it. Cost/benefit said no for one candidate.

## CEO audit

| check | result |
|---|---|
| LIVE deals | 10087 (was 10086) |
| max LIVE id | 10432 (this tick's push) |
| PENDING_REVIEW | 0 |
| LIVE null price / null image | 0 / 0 |
| posts/day IST | 09-16:3 09-17:3 **09-18:3** — target met |
| posts total | 313, coverless 0, seoless 0 |
| prod `/` `/offers` `/blog` `/sitemap.xml` `/feed.xml` `/api/deals` | 200 × 6 |

### Stall flag — DOWNGRADED

Previous two ticks flagged the indiafreestuff sweep as dead (max id stuck at 10431).
It is **not dead**: ids 10430 + 10431 landed at 06:27 UTC (11:57 IST), i.e. the `23 */2 * * *`
run at 06:23 UTC produced 2 deals. So the sweep runs — the yield collapsed from ~20-28/sweep
to 2. That is a source-side or filter-side regression, not a dead cron. Next run 08:23 UTC
(13:53 IST); if it is also ≤2, the filter regex or the IFS feed shape changed and needs a hand-run diff.

### ROT FLAGGED (carried, still open)

- **indiafreestuff yield collapse**: 2 deals/sweep vs the ~20-28 baseline. Reclassified from
  "stalled" to "collapsed yield" this tick. Needs a hand-run of `ingest-ifs-proper.mjs` with
  the reject counters printed, to see which filter is eating the cards.
- **`.claude/agents/deal-ingest.md` stale**: still says `"status": "pending-review"` (AUTO-APPROVE
  killed the review gate) and still names the EarnKaro flow for Flipkart (matrix is
  `affid=djhackraj` / Cuelinks for everything non-Amazon). Will mislead the next ingest run.
- **Amazon extraction fixes not baked into `apps/api/scripts/lib/ingest-common.mjs`**: buybox-first
  read, CSS-polluted-buybox fallback, MRP via `pay/(1-savingsPercentage)`, `#add-to-cart-button`
  as the in-stock signal. Re-derived by hand every tick. Highest-value unshipped code fix.
- **NEW: `productLd()` returns nothing on shopsy.in** — no `offers` in their ld+json. Any future
  Shopsy deal verified through the shared `productLd()` path will silently fail price verification.
  Worth a `finalPrice` fallback in `ingest-common.mjs` alongside the Amazon fixes.
- **`PWBGGD4THDQZYAY6` is in the seen-cache but not in the DB.** Same for an unknown number of
  the 1,699 entries. The cache records "processed", not "published", so a deal skipped once for
  a transient reason (price drift, dead link) is never reconsidered. Low priority, but it silently
  shrinks the addressable pool every tick.
- **`where to get free samples`: 11,072 impressions, pos 6.8, 0 clicks.** 46 of 313 slugs in that
  cluster. Merge/prune, not more posts. Still the highest-value unshipped SEO action.
- Organic still collapsed: last-28d GSC = 2 clicks / 67 impressions.
- 307 LIVE deals with pointless `updatedAt` re-stamps — undiagnosed.
- W6 (`/coupons` + `/freebies` ignore `?type=`) and W8 (`sku` from ASIN needs `productId` on the
  shared DTO) both still need an API change.
- ~605 untracked scratch files under `apps/api/` (git porcelain ~1589 lines).

### Owner decisions still open

1. Ratify publish-at-live-price + the 30% minimum live-discount floor in CLAUDE.md.
2. Permanent DB pool cap in `apps/api/.env`.
3. DesiDime Task Scheduler job `7,37 * * * *` (external cron, needs explicit go-ahead).
4. Free-samples cluster consolidation (46 of 313 slugs).
5. Scratch-file cleanup under `apps/api/`.

## TELEGRAM-DEAL-MONITOR tick — 14:0x IST

Sidebar read in one `browser_evaluate` over `.chat-list`, 13 groups from `data/tg-groups.json`.
ONLINE SHOPPING DEALS opened in-chat (sidebar row showed 1 deal, the open chat showed 5 — 4 of them new).

Funnel: **12 discovered → 3 dropped as live-DB dupes → 8 verified → 2 rejected on price drift → 6 pushed LIVE.**

### Published (all Amazon, `status:live`, real `m.media-amazon.com` hero, price verified in the logged-in tab)

| ASIN | product | price | MRP | off | slug |
|---|---|---|---|---|---|
| B085YB82MT | Nirlon ceramic fry pan + kadhai set of 2 | ₹2,079 | ₹6,400 | 68% | `nirlon-ceramic-non-stick-fry-pan-and-kadhai-set-of-2-with-glass-lid-b085yb` |
| B0CJF7FFLW | Gang's 19L laptop backpack (15.6") | ₹759 | ₹13,999 | 95% | `gang-s-19l-laptop-backpack-for-15-6-inch-laptops-b0cjf7` |
| B0DDZ3KLWD | Lenskart BLU zero-power computer glasses | ₹499 | ₹3,500 | 86% | `lenskart-blu-zero-power-anti-glare-computer-glasses-b0ddz3` |
| B07QRRH98D | No-show low-cut socks, 6 pairs | ₹239 | ₹899 | 73% | `no-show-low-cut-socks-for-women-pack-of-6-b07qrr` |
| B0B4DJJV5X | Sirona menstrual cup M + steam steriliser + wash | ₹289 | ₹2,547 | 89% | `sirona-reusable-menstrual-cup-medium-with-steam-sterilizer-and-wash-b0b4dj` |
| B09YVRGRFZ | Sirona menstrual cup L + collapsible steriliser, 2pk | ₹289 | ₹748 | 61% | `sirona-reusable-menstrual-cup-large-with-collapsible-sterilizer-pack-of-2-b09yvr` |

Bulk push: **HTTP 201, count 6, all `+NEW`.**
IndexNow: **`DONE: IndexNow -> HTTP 200 for 8 urls`** (6 slugs + `/` + `/offers`).
Seen-cache: `data/tg-multi-seen.json` **1700 → 1705** (11 ids offered, only 6 new).

### Rejected on price drift

- **B0D6ZRKPV2** (Dealzone, posted ₹147) — live **₹579** vs MRP ₹597, 3% off. Product is Dr. Pets Treetos chicken dog treats 3-pack.
- **PWBGGD4THDQZYAY6** Syska 10000 mAh power bank (Loot Deals 24x7, posted ₹799) — live **₹1,323** vs MRP ₹1,393, ~5% off.

### Dropped as live-DB duplicates

B0G38DGNKM (deal 7110, ₹3,653) · B07QX21WZQ (deal 5825, ₹549) · B0G5PN6HHY (deal 102, ₹5,199).

### NEW FINDING — the correct Amazon price selector

`#corePriceDisplay_desktop_feature_div .priceToPay .a-offscreen` returned **empty on all 7 PDPs tested**.
`.priceToPay .a-price-whole` returned the exact live price on all 7. That is the extractor to bake in.

```js
const box = d.querySelector('#corePriceDisplay_desktop_feature_div') || d.querySelector('#corePrice_feature_div');
const price = box.querySelector('.priceToPay .a-price-whole').textContent.trim();  // RELIABLE
const mrp   = box.querySelector('.basisPrice .a-offscreen').textContent;           // RELIABLE
const sav   = box.querySelector('.savingsPercentage').textContent;                 // "-68%"
const stock = !!d.querySelector('#add-to-cart-button');
const img   = d.querySelector('#landingImage').getAttribute('data-old-hires');
```

Buybox `textContent` starts with raw CSS (`.ppu-align-bottom, .stp-ppu-adaptive-container { … }`) — that pollution is why naive
`.a-price .a-offscreen` scoping grabs a stray `.a-text-price` node and yields **inverted price/MRP pairs** (B0D6ZRKPV2 first read
back as `price 579 / mrp 227`).

### NEW FINDING — Flipkart

Plain curl with a Chrome UA → **403**. curl with the full browser header set (Accept / Accept-Language / Upgrade-Insecure-Requests /
Sec-Fetch-*/ `--compressed`) → **still 403, size 787**. A Playwright tab gets 200 — but the PDP carries **no
`<script type="application/ld+json">` Product block**, so the documented `productLd()` path does not work on Flipkart at all.
Fallback that worked: `document.body.innerText.match(/₹[\d,]+/g)` + rukminim CDN image from the `img` list.

### CEO audit

| check | result |
|---|---|
| LIVE deals | 10,116 (max LIVE id 10461, created 08:28 UTC) |
| PENDING_REVIEW | 0 |
| LIVE null price / null image | 0 / 0 |
| posts published | 313, **coverless 0, seoless 0** |
| posts/day IST last 4 | 09-15:3 09-16:3 09-17:3 **09-18:3** — floor met |
| broadcast cursor vs DB max | `lastId:10455` vs max LIVE 10461 — **6 behind**, next tg-broadcast run clears it |
| prod `/` `/offers` `/blog` `/sitemap.xml` `/feed.xml` `/api/deals` | 200 × 6 |
| unpushed commits | none before this one |

### ROT FLAGGED (carried, still open)

- ~~222 LIVE rows re-stamping `updatedAt` with no content change — undiagnosed~~ **CLOSED, not rot.** Writer is
  `apps/api/src/redirect/redirect.controller.ts:28` — `clickCount: { increment: 1 }` against a `@updatedAt` column, so every
  outbound affiliate click re-stamps the row. Verified: 74 Click rows / 60 distinct deals in the same hour as 53 restamps,
  1-7/min continuous, id range 702→10431 — organic traffic, not a timer. Harmless for SEO: `apps/web/src/app/sitemap.ts:79`
  derives deal `lastModified` from `priceHistory[0].postedAt ?? createdAt`, never `updatedAt`. Do not re-flag; `updatedAt` is
  simply not a content-change signal on `Deal`.
- **Amazon extraction fixes STILL not in `apps/api/scripts/lib/ingest-common.mjs`** — now with a concrete, tested selector
  (`.priceToPay .a-price-whole`) plus a Flipkart no-ld+json fallback. Re-derived by hand 5 ticks running. Highest-value unshipped
  code fix on the repo.
- **`.claude/agents/deal-ingest.md` stale on four points**: `"status": "pending-review"` (AUTO-APPROVE killed the gate), EarnKaro
  named for Flipkart **and** for "Other stores" (matrix is `affid=djhackraj` / Cuelinks), `data/deals/index.json` dedup (superseded
  by live-DB dedup), placeholder `"amazonTag": "REPLACE-WITH-OUR-TAG-21"`.
- **tg seen-cache records "processed", not "published"** — third concrete case this week: 5 of 11 ids were already cached, two of
  which were never published. A rejected deal can never be re-evaluated when its price improves.
- **`where to get free samples`: 11,072 impressions, pos 6.8, 0 clicks.** 46 of 313 slugs in that cluster. Merge/prune, not posts.
- Organic still collapsed: last-28d GSC = 2 clicks / 67 impressions.
- W6 (`/coupons` + `/freebies` ignore `?type=`) and W8 (`sku` from ASIN needs `productId` on the shared DTO) both need an API change.
- ~605 untracked scratch files under `apps/api/` (git porcelain ~1,589 lines).

### Owner decisions still open

1. Ratify publish-at-live-price + the 30% minimum live-discount floor in CLAUDE.md.
2. Permanent DB pool cap in `apps/api/.env`.
3. DesiDime Task Scheduler job `7,37 * * * *` (external cron, needs explicit go-ahead).
4. Free-samples cluster consolidation (46 of 313 slugs).
5. Scratch-file cleanup under `apps/api/`.

## 14:5x IST tick (TICK J)

Sidebar read in one `browser_evaluate` over `.chat-list` (tab 0 selected first — the
evaluate returns `[]` unless the Telegram tab is current). 13 groups from
`data/tg-groups.json`, plus a 6-message deep read of the open ONLINE SHOPPING DEALS chat.

### Pushed — 2 deals, both Amazon, both verified live

| ASIN | product | price | MRP | off | slug |
|---|---|---|---|---|---|
| B007E9M6EI | Maybelline New York Lip Balm (Baby Lips, 4g) | ₹92 | 199 | 54% | `maybelline-new-york-lip-balm-...-b007e9` |
| B09SLFSNW4 | Cutting EDGE Interlocking Drawer Organizer (Set of 14, Black) | ₹399 | 529 | 25% | `cutting-edge-interlocking-drawer-organizer-set-of-14-black-b09slf` |

- `/admin/deals/bulk` → **HTTP 201, count 2**.
- **IndexNow → HTTP 200 for 4 urls** (2 deal slugs + `/` + `/offers` hubs).
- Both pages live on prod: **200 / 200**.
- Images are real `m.media-amazon.com` CDN (`51hH-sA1CFL._SL1060_`, `71rZTW8ioEL._SL1500_`).

### Verification method (new, cheaper)

Instead of one `browser_navigate` per ASIN, **one** `browser_evaluate` inside the
already-logged-in Amazon tab doing a same-origin fetch + DOMParser per ASIN:

```js
const r = await fetch(`https://www.amazon.in/dp/${a}?th=1&psc=1`, { credentials: 'include' });
const doc = new DOMParser().parseFromString(await r.text(), 'text/html');
```

HTTP 200, every field parses (`#productTitle`, `.priceToPay .a-price-whole`,
`.basisPrice .a-offscreen`, `.savingsPercentage`, `#add-to-cart-button`,
`#landingImage[data-old-hires]`). N ASINs, zero navigations. This supersedes the
per-ASIN navigate loop and should be what goes into `ingest-common.mjs`.

### Rejected / skipped

| candidate | reason |
|---|---|
| B0G38DGNKM, B07QX21WZQ, B07QRRH98D, B0B4DJJV5X, B09YVRGRFZ, B0G5PN6HHY, B0DDZ3KLWD, B0CJF7FFLW | already LIVE in DB (ids 7110, 5825, 10459, 10460, 10461, 102, 10458, 10457) |
| Flipkart Syska 10000 mAh power bank `PWBGGD4THDQZYAY6` | **price mismatch** — TG claimed ₹799, live PDP reads ₹1,323 with an out-of-stock string. Dropped. |
| Dealdost, Deal Dibba | multi-product / loot posts |
| IndiaFreeStuff Tips & Tricks | Instamart loot + `/search?query=` category link |
| Hidden Loot Deals, OMG LOOTDEALS | freebie/earn-money posts, no product |
| ONLINE SHOPPING DEALS stockbroker ad, iPhone-rate channel, our own channel, DMs/bots | not deal sources |

### Shortlink resolution — HEAD is useless

`curl -sIL` (HEAD) returns the **shortener URL itself** for `link.amazon/*` and
`rogerkart.com/r/*`. `curl -sL -o /dev/null -w '%{url_effective}'` (GET) resolves
`link.amazon` fine. `rogerkart.com/r/*` resolves for **neither** — it is a Next.js
client-side-JS Amazon shortener (`robots noindex,nofollow`); only a real browser tab
plus a ~4s wait lands on the `/dp/` URL. Use GET, and a browser tab for rogerkart.

### CEO audit

| check | result |
|---|---|
| LIVE deals | 10118 (was 10086 at the 12:4x tick → **ingest is no longer stalled**, +32) |
| max LIVE id | 10463 |
| broadcast cursor | `lastId:10463` — caught up |
| PENDING_REVIEW | 0 |
| LIVE null price / null image | 0 / 0 |
| posts published | 313, coverless **0**, seoless **0** |
| posts/day IST last 4 | 09-15:3 09-16:3 09-17:3 **09-18:3** — target met |
| prod `/` `/offers` `/blog` `/sitemap.xml` `/feed.xml` `/api/deals` | 200 × 6 |

### ROT — closed this tick

- **Deal ingest stalled** (flagged 12:4x): closed. LIVE 10086 → 10118.
- **`updatedAt` re-stamps on ~307 LIVE deals**: closed, and it was never rot. Writer is
  `apps/api/src/redirect/redirect.controller.ts:28` — `{ clickCount: { increment: 1 } }`
  on a model with `updatedAt @updatedAt`, so every outbound affiliate click re-stamps.
  Proved with 74 `Click` rows across 60 distinct deals in the same hour as 53 restamps.
  Harmless for SEO: `apps/web/src/app/sitemap.ts:79` uses `priceHistory[0].postedAt ?? createdAt`,
  never `updatedAt`. Recorded in memory — do not re-flag.

### ROT — still open

- **Amazon/Flipkart extraction still not in `apps/api/scripts/lib/ingest-common.mjs`.** Third
  consecutive tick the selector set was re-derived by hand. Now also owes the same-origin
  fetch+DOMParser verifier, a shopsy `finalPrice` fallback, a Flipkart no-ld+json browser
  fallback, and an implausible-MRP guard (`.basisPrice` served ₹13,999 against a ₹759 price
  on B0CJF7FFLW = a fake −95% badge). **Highest-value unshipped code fix.**
- **The tg seen-cache records "processed", not "published".** `PWBGGD4THDQZYAY6` was in
  `data/tg-multi-seen.json` but absent from the DB. The cache cannot be trusted as a
  publish record — the DB dedup pass is load-bearing and must stay.
- `.claude/agents/deal-ingest.md` stale on four points: placeholder `amazonTag`,
  `earnkaroPrefix`, Flipkart/other-store routing through EarnKaro, and `status: pending-review`.
- `where to get free samples`: 11,072 impressions / pos 6.8 / **0 clicks**; 46 of 313 slugs
  in that cluster. Needs merge/prune, not more posts.
- Organic collapse: last-28d GSC = 2 clicks / 67 impressions.
- W6 (`/coupons` + `/freebies` ignore `?type=`) and W8 (`sku` from ASIN needs `productId`
  on the shared DTO) both need an API change.
- ~605 untracked scratch files under `apps/api/`.

### Owner decisions still open

1. Ratify publish-at-live-price + the 30% minimum live-discount floor in CLAUDE.md.
2. Permanent DB pool cap in `apps/api/.env`.
3. DesiDime Task Scheduler job `7,37 * * * *` (external cron, needs explicit go-ahead).
4. Free-samples cluster consolidation (46 of 313 slugs).
5. Scratch-file cleanup under `apps/api/`.

## 15:5x IST tick

Sidebar read in ONE `browser_evaluate` over `.chat-list .ListItem.Chat` (14 rows = 13 source
groups + 1 bot row). One deeper read on the open `ONLINE SHOPPING DEALS` chat
(`.message-content .text-content`, last 6). No chat-list reload, no navigation.

### Funnel

| stage | n |
|---|---|
| sidebar rows read | 14 |
| non-single-product posts skipped | 6 |
| shortlinks resolved (`curl -sL`) | 12 |
| candidate product ids | 9 |
| dedup drops (DB LIVE) | 5 |
| verification rejects | 1 |
| **pushed LIVE** | **3** |

### Skipped as not-single-product

| group | post | why |
|---|---|---|
| Dealdost | Myntra VIP ticket | not a product |
| Deal Dibba | "smartwatches from ₹999" boAt/Noise/Fire-Boltt | multi-product loot |
| IndiaFreeStuff Tips & Tricks | Swiggy Instamart `?query=` link | search page |
| Hidden Loot Deals & Offers | Blinkit free cold coffee | freebie, no product page |
| OMG LOOTDEALS | "video dekho paisa kamao" | junk |
| SB Loots And Deals Help Bot | — | bot row, not a source group |

### Shortlink resolution

| shortlink | resolved | verdict |
|---|---|---|
| `link.amazon/B05RuPWFM` | `/dp/B08MLJGB8W` | **KEEP** |
| `link.amazon/B05yvriRF` | `/dp/B0G38DGNKM` | DB dup #7110 |
| `link.amazon/B04QNP675` | `/dp/B09YVRGRFZ` | DB dup #10461 |
| `link.amazon/B0hhm2R1h` | `/dp/B0G5PN6HHY` | DB dup #102 |
| `link.amazon/B04NQ7AQJ` | `/dp/B0DDZ3KLWD` | DB dup #10458 |
| `link.amazon/B00daUC2t` | `/dp/B0GZ4VXTVB` | **KEEP** |
| `link.amazon/B0a2n4R0a` | `/dp/B0CL4BR6BS` | **KEEP** |
| `amzn.to/4hfrKNV` | `/dp/B0F2GQ5HDR` | DB dup #6685 |
| `amzn.to/4uZXfjK` | `/dp/B07QX21WZQ` | DB dup #5825 |
| `fkrt.co/l5KOxl` | `/p/itm4cfc25dfd4dc7?pid=PWBGGD4THDQZYAY6` | real product → rejected on verify |
| `fkrt.it/ZTDo1EuuuN` | `/flipkart/p/item?pid=CSDGGWS9NHF7HNGK` | tracking landing, **reject** |
| `rogerkart.com/r/pVQu7jR` | self-referential under curl | unresolvable, dropped |

### Verification reject

- **Syska 10000 mAh power bank** (`PWBGGD4THDQZYAY6`, Loot Deals 24x7 claimed ₹799).
  Flipkart serves **no `ld+json` Product block** to the browser either — fell back to DOM
  read in a Playwright tab: live price **₹1,323**, plus a Sold Out signal on the page.
  66% upward drift vs the post → rejected, not published.

### Pushed LIVE — `/admin/deals/bulk` **HTTP 201 count 3**

| ASIN | product | ₹ | MRP | off | slug |
|---|---|---|---|---|---|
| B08MLJGB8W | Sehaz Artworks wooden key holder, 7 hooks 2-bird | 64 | 899 | -93% | `…key-chain-b08mlj` |
| B0GZ4VXTVB | Hangover Desire perfume for men 50 ml EDP | 299 | 699 | -57% | `…oud-spicy--b0gz4v` |
| B0CL4BR6BS | Wonderchef vegetable cleaner + salad spinner | 299 | 1100 | -73% | `…pesticid-b0cl4b` |

All three verified in the logged-in Amazon tab in ONE `browser_evaluate` (same-origin
`fetch` + `DOMParser`, zero navigations): `#add-to-cart-button` present on all three, real
`m.media-amazon.com` CDN image from `#landingImage[data-old-hires]`, price off
`.priceToPay .a-price-whole` with the trailing dot stripped (`"64."` → 64).

B08MLJGB8W kept its ₹899 MRP: Amazon's own `.savingsPercentage` badge independently reads
**-93%**, matching `.basisPrice`, so the implausible-MRP guard was not triggered. The
Dealzone post quoted ₹71; live read was ₹64 → published at the live price.

- **IndexNow: `DONE: IndexNow -> HTTP 200 for 5 urls`** (3 deals + `/` + `/offers`).
- Prod spot-check: all 3 slugs **200**.
- `data/tg-multi-seen.json` 1707 → **1711** (3 pushed + the rejected Flipkart tracking pid).

### CEO audit

| check | result |
|---|---|
| prod `/` `/offers` `/blog` `/sitemap.xml` `/feed.xml` `/api/deals` `/llms.txt` | **200 × 7** (0.11–0.53s) |
| deals LIVE / PENDING_REVIEW / EXPIRED | 10136 / **0** / 257 |
| LIVE null price / null image | 0 / 0 |
| posts published / coverless / seoless | 313 / **0** / **0** |
| posts per day IST (last 4) | 09-15:3 09-16:3 09-17:3 09-18:3 |
| broadcast cursor vs max LIVE id | `lastId:10478` vs 10481 — 3 behind, exactly this batch; next broadcast run clears it |
| unpushed commits | 0 |

Nothing broken outside the tick, so nothing was fixed inline.

### ROT FLAGGED (carried, still open)

- **`apps/api/scripts/lib/ingest-common.mjs` still has no Amazon extractor.** Re-derived by
  hand for the **6th** consecutive tick. It owes: the same-origin `fetch` + `DOMParser`
  verifier, `.priceToPay .a-price-whole` with trailing-dot strip, `.basisPrice .a-offscreen`
  MRP + implausible-MRP guard, `.savingsPercentage`, `#add-to-cart-button` in-stock,
  `data-a-dynamic-image` image fallback — **and now also a Flipkart DOM fallback**, because
  this tick confirmed Flipkart serves no `ld+json` Product block to a real browser either,
  not only to curl. Highest-value unshipped code fix on the repo.
- **Flipkart is effectively a dead source across every ingest.** indiafreestuff resolves every
  Flipkart link to `dl.flipkart.com/dl/indiafreestuff/p/indiafreestuff`; SB Loots resolves to
  `/flipkart/p/item`; the one genuine `/p/itm…` product this tick failed price verification.
  Zero Flipkart deals published in the last three ticks.
- tg seen-cache records "processed", not "published" — `PWBGGD4THDQZYAY6` was cached as seen
  yet absent from the DB. DB dedup stays load-bearing; the cache alone would have leaked it.
- `.claude/agents/deal-ingest.md` stale on 4 points (placeholder Amazon tag, EarnKaro prefix,
  `data/deals/index.json` dedup, `pending-review` status).
- `where to get free samples`: 11,072 impressions / pos 6.8 / **0 clicks**; 46 of 313 slugs in
  that cluster. Needs merge/prune, not more posts.
- Organic collapse: last-28d GSC = 2 clicks / 67 impressions.
- W6 (`/coupons` + `/freebies` ignore `?type=`) and W8 (`sku` from ASIN needs `productId` on
  the shared DTO) both need an API change.
- ~605 untracked scratch files under `apps/api/`.

### Owner decisions still open

1. Ratify publish-at-live-price + the 30% minimum live-discount floor in CLAUDE.md.
2. Permanent DB pool cap in `apps/api/.env`.
3. DesiDime Task Scheduler job `7,37 * * * *` (external cron, needs explicit go-ahead).
4. Free-samples cluster consolidation (46 of 313 slugs).
5. Scratch-file cleanup under `apps/api/`.

## 16:5x IST tick

One `browser_evaluate` over `.chat-list .ListItem.Chat` (25 rows). No navigation in the
Telegram tab, no chat reload. The Telegram service row is a non-source chat and was skipped
without reading its contents.

### Funnel

| stage | n |
|---|---|
| sidebar rows read | 25 |
| deal-group rows carrying a link | 10 |
| already-processed links (unchanged since 15:5x) | 4 |
| new shortlinks to resolve | 3 |
| resolved to a product | 2 |
| dedup drops (DB LIVE) | 1 |
| **pushed LIVE** | **1** |

### Shortlink resolution

| shortlink | group | resolved | verdict |
|---|---|---|---|
| `amzn.to/4j4VnUw` | CoolzTricks Official | `/dp/B07RF88GKB` | **KEEP** |
| `rogerkart.com/r/pVQu7jR` | Rogerkart Deals | `/dp/B0CJF7FFLW` | DB dup #10457 |
| `amzn.lt/K1AtCZUG` | SB Loots And Deals | **NXDOMAIN** | unresolvable, dropped |

`rogerkart.com/r/…` was logged as unresolvable last tick. It is resolvable — the redirect is
client-side, so `curl -sL -w %{url_effective}` returns the shortlink itself. Fix: grep the
response **body** for an `amazon.in|flipkart.com|myntra|ajio|croma|nykaa` URL instead of
following the redirect. That worked first try and should go into the shared lib.

`amzn.lt` no longer resolves at all — `ERR_NAME_NOT_RESOLVED` from both curl and a real
browser navigation. Every SB Loots And Deals post routes through it, so that group is dead as
a source until they change shortener.

### Skipped as not-single-product

Dealdost (Myntra VIP ticket, not a product) · LATEST IPHONE RATES (grey-market reseller rate
card) · Deal Dibba (multi-brand smartwatch loot) · IndiaFreeStuff Tips (Swiggy Instamart
`?query=` search) · Hidden Loot (Blinkit freebie) · OMG LOOTDEALS (junk) · Dhunu sonowal
(creator-app spam) · SB Loots Help Bot (bot row) · RichDeals + ONLINE SHOPPING DEALS (our own
broadcast output, echoing the Wonderchef salad spinner pushed at 15:5x) · 4 private DMs.

Dealzone `B05RuPWFM`, INDIAN CHEAP DEALS `B05yvriRF`, NonStopDeals `4uZXfjK` and
Loot Deals 24x7 `fkrt.co/l5KOxl` are all unchanged from the 15:5x tick — already pushed,
deduped or rejected there. Not re-resolved.

### Pushed LIVE — `/admin/deals/bulk` **HTTP 201 count 1**

| ASIN | product | ₹ | MRP | off | slug |
|---|---|---|---|---|---|
| B07RF88GKB | Wonderchef Hydro Bot 1 L single-wall steel bottle | 249 | 750 | -67% | `…single-wal-b07rf8` |

Verified on the live PDP in the logged-in tab: `#add-to-cart-button` present, real
`m.media-amazon.com` image off `#landingImage[data-old-hires]`, `.priceToPay` ₹249 — exactly
the price CoolzTricks posted, no drift. `.savingsPercentage` reads -67%, matching the
₹750 `.basisPrice` MRP, so no implausible-MRP question. Clears the 30% floor.

- **IndexNow: `DONE: IndexNow -> HTTP 200 for 3 urls`** (1 deal + `/` + `/offers`).
- Prod spot-check: slug **200**.
- `data/tg-multi-seen.json` 1711 → **1713**.

### CEO audit

| check | result |
|---|---|
| deals LIVE / PENDING_REVIEW / EXPIRED | 10137 / **0** / 257 |
| LIVE null price / null image | 0 / 0 |
| max LIVE id | 10482 |
| posts published / coverless / seoless | 313 / **0** / **0** |
| posts per day IST | 09-15:3 09-16:3 09-17:3 09-18:3 |
| broadcast cursor | `lastId:10481` vs 10482 — 1 behind, this push; next run clears |
| unpushed commits | 0 |

Nothing rotting outside the tick, so nothing was fixed inline.

### ROT FLAGGED

- **NEW: `amzn.lt` is dead (NXDOMAIN).** SB Loots And Deals is unusable until they switch
  shortener. Confirmed from curl *and* a browser navigation, so it is the domain, not bot
  blocking.
- **NEW (and fixed in method, not in code): client-side shortlink resolution.** `rogerkart.com`
  — and likely any JS-redirect shortener — needs a body grep, not a redirect follow. Belongs in
  the shared lib next to the Amazon extractor.
- `apps/api/scripts/lib/ingest-common.mjs` still has no Amazon extractor. Hand-re-derived for
  the **7th** consecutive tick. Now owes, in total: the same-origin `fetch` + `DOMParser`
  verifier, `.priceToPay .a-price-whole` trailing-dot strip, `.basisPrice` MRP + implausible-MRP
  guard, `.savingsPercentage`, `#add-to-cart-button`, `data-a-dynamic-image` fallback, a
  Flipkart DOM fallback, **and the body-grep shortlink resolver**.
- Recurring trap re-hit this tick: the same-origin Amazon verifier must run on an
  **amazon.in** tab. A failed navigation left tab 1 on `chrome-error://`, and the verifier
  returned `TypeError: Failed to fetch` for the ASIN until the tab was pointed back at a PDP.
- Flipkart still dead as a source across all three ingests.
- `.claude/agents/deal-ingest.md` stale on 4 points.
- `where to get free samples`: 11,072 impressions / pos 6.8 / **0 clicks**; 46 of 313 slugs.
- Organic last-28d: 2 clicks / 67 impressions.
- W6 and W8 both need an API change. ~605 untracked scratch files under `apps/api/`.

### Owner decisions still open

1. Ratify publish-at-live-price + the 30% minimum live-discount floor in CLAUDE.md.
2. Permanent DB pool cap in `apps/api/.env`.
3. DesiDime Task Scheduler job `7,37 * * * *`.
4. Free-samples cluster consolidation (46 of 313 slugs).
5. Scratch-file cleanup under `apps/api/`.

## 17:4x IST tick (TELEGRAM-DEAL-MONITOR)

One `browser_evaluate` over `.chat-list .ListItem.Chat` → **25 rows** read in a single call
(no chat opened, no reload). Triage: 6 candidate single-product posts, 19 skipped.

### Funnel

| stage | n |
|---|---|
| sidebar rows read | 25 |
| single-product candidates | 6 |
| shortlinks resolved | 6 (1 only via body-grep) |
| fresh after dedup (seen-cache + live DB) | 2 |
| verified in logged-in Amazon tab | 2 |
| pushed live | **2** |

### Shortlink resolution

| group | card | link | resolved |
|---|---|---|---|
| ONLINE SHOPPING DEALS | Myx Women Palazzo ₹199 | `link.amazon/…` | B0B9G9J7WD |
| CoolzTricks Official | Aristocrat Duffle ₹445 | `amzn.to/3V3G04M` | B0CWSCT2VJ |
| Rogerkart Deals | Gang's Laptop Bag ₹759 | `rogerkart.com/r/…` | B0CJF7FFLW — **body-grep only** (curl final URL returned itself) |
| INDIAN CHEAP DEALS | Ladies Handbag ₹3,500 | `link.amazon/…` | B0G38DGNKM |
| Loot Deals 24x7 | Syska 10000 mAh ₹799 | `fkrt.co/l5KOxl` | Flipkart `PWBGGD4THDQZYAY6` |
| NonStopDeals | "151" | `amzn.to/4uZXfjK` | B07QX21WZQ |

### Dedup (seen-cache + live DB, `productId` OR `affiliateUrl contains`)

| id | DB | seen | verdict |
|---|---|---|---|
| B0B9G9J7WD | — | no | **fresh** |
| B0CWSCT2VJ | — | no | **fresh** |
| B0CJF7FFLW | #10457 LIVE | yes | dup |
| B0G38DGNKM | #7110 LIVE | yes | dup |
| PWBGGD4THDQZYAY6 | — | yes | dup (already processed, not published — junk/loot) |
| B07QX21WZQ | #5825 LIVE | yes | dup |

Four of six were already ours. Telegram groups mostly recycle what indiafreestuff and
DesiDime already surfaced — this source is a thin top-up, not a primary.

### Pushed (HTTP 201, count 2)

| ASIN | ₹ | MRP | off | product |
|---|---|---|---|---|
| B0B9G9J7WD | 199 | 999 | 80% | Amazon Brand Myx Women Relaxed Fit Mid Rise Palazzo (plus sizes) |
| B0CWSCT2VJ | 468 | 2080 | 78% | Aristocrat Enigma 52 cm Polyester Softsided Cabin Duffle Bag, Red |

Both in stock (`#add-to-cart-button` present), both images from `m.media-amazon.com`,
both tagged `ashoksachdev-21` on `/dp/ASIN`.

**Price drift:** the CoolzTricks card said ₹445, the live PDP read **₹468** (+₹23, 5%).
Published at the verified live price, not the card price — the 78% discount is real and
holds on the live page. Contrast with TICK P's B0D5HQLLGR rejection (₹138 → ₹289, +109%),
which was outside any plausible tolerance. The ±₹1 rule is still unratified in CLAUDE.md;
this tick treats live-verified price as the source of truth and records the drift.

### Skipped (19)

Dealzone (Puma footwear, category), RichDeals (our own channel), SB Loots And Deals
(`amzn.lt` NXDOMAIN — dead host), Dealdost (Myntra VIP ticket, not a product), 𝗟𝗔𝗧𝗘𝗦𝗧
𝗜𝗣𝗛𝗢𝗡𝗘 𝗥𝗔𝗧𝗘𝗦 (afyp.in, no product page), Dhunu sonowal (spam), Deal Dibba
("Loot:" multi-product), IndiaFreeStuff Tips & Tricks (Instamart loot/search), Hidden
Loot Deals (Blinkit freebie), the Telegram service row (non-source, skipped), plus 9
personal chats and bots.

### Freshness

- IndexNow: **HTTP 200 for 4 urls** (2 slugs + `/` + `/offers`).
- Both slugs live on prod: **200 / 200**.
- `sitemap.xml` ISR 1800s; `llms.txt` force-dynamic — no hub route added, nothing to edit.

### CEO audit

| check | result |
|---|---|
| deals LIVE / EXPIRED | 10155 / 257 (+2 this tick) |
| LIVE null price / null image | 0 / 0 |
| PENDING_REVIEW | 0 |
| prod `/api/deals` head id | **10500** = DB max LIVE id — push reached prod |
| prod `/` `/offers` `/blog` `/sitemap.xml` `/feed.xml` `/api/deals` | 200 × 6 |
| posts published | 313, coverless 0, seoless 0 |
| posts/day IST last 4 | 09-15:3 09-16:3 09-17:3 09-18:3 — rule met |
| broadcast cursor | `lastId:10482` vs max 10500 — 18 behind, external tg-broadcast cron will catch up |
| `data/tg-multi-seen.json` | 1713 → 1715 |

### ROT FLAGGED (carried)

- **`apps/api/scripts/lib/ingest-common.mjs` still missing the Amazon extractor** — hand
  re-derived for the **10th tick running**. Also owes: same-origin fetch + DOMParser
  verifier, implausible-MRP guard, `data-a-dynamic-image` image fallback, trailing-dot
  price strip, shopsy `finalPrice` fallback, Flipkart browser-tab fallback, and the
  shortlink **body-grep** fallback. Highest-value unshipped code fix.
- **`curlFinal` cannot follow client-side redirects.** Cost 7 of 35 links on TICK P;
  `rogerkart.com/r/…` needed the body-grep path again this tick. The fallback is proven
  twice over and still not in the shared lib.
- **Telegram yield is 2 of 25 rows (8%)**, and 4 of 6 candidates were already in the DB.
  indiafreestuff remains the load-bearing source.
- `amzn.lt` NXDOMAIN — SB Loots And Deals permanently unusable.
- indiafreestuff Flipkart links structurally dead (`dl.flipkart.com/dl/indiafreestuff/…`).
- tg seen-cache records "processed", not "published" — DB dedup stays load-bearing
  (PWBGGD4THDQZYAY6 proves it: seen, never published).
- `.claude/agents/deal-ingest.md` stale on 4 points (placeholder Amazon tag, EarnKaro
  prefix, `data/deals/index.json` dedup, `status: pending-review`).
- `where to get free samples`: 11,072 impressions / pos 6.8 / **0 clicks**; 46 of 313
  slugs in that cluster. Needs merge/prune, not more posts.
- Organic collapsed: last-28d GSC = 2 clicks / 67 impressions.
- W6 (`/coupons` + `/freebies` ignore `?type=`) and W8 (`sku` from ASIN needs `productId`
  on the shared DTO) both need an API change.
- ~605 untracked scratch files under `apps/api/`.

### Owner decisions still open

1. Ratify publish-at-live-price + the 30% minimum live-discount floor in CLAUDE.md.
2. Permanent DB pool cap in `apps/api/.env`.
3. DesiDime Task Scheduler job `7,37 * * * *` (external cron, needs explicit go-ahead).
4. Free-samples cluster consolidation (46 of 313 slugs).
5. Scratch-file cleanup under `apps/api/`.
