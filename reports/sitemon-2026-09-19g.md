# SITEMON + CEO audit tick — 2026-09-19g (IST)

**7/7 prod endpoints 200, no outlier, every audit arm clean. Nothing needed an inline fix.**

That last sentence is the finding. The previous tick (`09-19f`) made the largest inline repair any
tick has made — 89 backfilled `discountPct` rows — and this tick confirms the repair held and
produced no successors.

## Uptime / status

```
200 0.434  /
200 0.123  /offers
200 0.512  /blog
200 0.384  /sitemap.xml
200 0.167  /feed.xml
200 0.121  /api/deals
200 0.371  /llms.txt
```

**No sample out of band — the first tick in several with nothing to re-measure.** `/offers` is back
at 0.123 s after its 1.162 s reading last hour, which retroactively confirms that disposition
(cold start, closed, not rot). The slow-sample rule now stands at **three re-measurements, three
false alarms**, and this tick is the first where it did not need to run at all.

## Live deal count sanity — prod vs DB

```
prod /api/deals?limit=1 → id 10687
  slug  longway-wave-p1-1200-mm-48-inch-ultra-high-speed-3-blade-anti-dust-decorative-ce-b0cqyw38lh
  title … at ₹1599 (51% Off) – Amazon
DB   max deal id        → 10687 LIVE ₹1599
```

**Exact agreement on id, slug and price.** That deal was pushed this same hour in
`telegram-2026-09-19N` and prod already serves it — no deploy, no lag, which is the expected
behaviour since the web app reads the managed DB through the prod API.

**The `₹` glyph renders correctly in the served title.** Checked deliberately: the `Rs.`-instead-of-`₹`
defect from an earlier tick shipped through a title string, so the served title is where it would
reappear. It did not.

**Sitemap `<loc>` count 9640**, up from 9604 at `09-19f` — **+36**. The ISR `revalidate = 1800`
window turned over and the recent batch is carried. Last tick noted deal 10652 had not landed yet
and that the 4 newly-indexable rows would appear on the same turnover; both did.

## Rot #29 — the backfill held

```
LIVE discountPct NULL w/ price+mrp: 0
```

**Zero**, against 89 an hour ago. The backfill is holding and **no new instance appeared in the
hour since**, across 36 newly-carried URLs.

One hour is not proof. Nothing in the ingest path *guarantees* the column is written when price
and MRP are known, so the class stays open — but this is the first positive evidence, and it
supports making the arm permanent rather than treating it as a one-off sweep.

**Recommendation restated: the `discountPct IS NULL AND price IS NOT NULL AND mrp IS NOT NULL`
arm is now a standing member of the CEO audit set.** It ran again this tick unprompted and cost
one query.

## CEO audit (verified against the DB)

| Check | Result |
|---|---|
| Deals | LIVE **10340** · PENDING_REVIEW **0** · EXPIRED **259** |
| LIVE null price / null image | **0 / 0** (ninth consecutive no-op) |
| LIVE discountPct null w/ price+mrp | **0** — #29's backfill holding |
| DB max deal | **10687** — matches prod on id, slug and price |
| Posts/day IST (8 d) | 09-12:3 · 09-13:4 · 09-14:4 · 09-15:3 · 09-16:3 · 09-17:3 · 09-18:3 · **09-19:3** |
| Today (IST) | **3** — inside the 2-3 target, under the cap of 4, never 0 |
| Blog hygiene | published **316** · noCover 0 · noSeoTitle 0 · noSeoDesc 0 |
| Ingest pace | **226 / 24 h**, **951 / 7 d ≈ 136/day** |
| tg-broadcast cursor | 10482 vs 10687 — **drift 205** (rot #4) |
| Unpushed commits | **0** before this tick |

PENDING_REVIEW is 0 **by absence** from `deal.groupBy({by:['status']})`, not by a zero row — an
absent key and a broken query look identical in that output, so it gets said every tick.

**Post counts here use `publishedAt IS NOT NULL`, not a `status` column.** The first attempt at
this audit threw `Unknown argument 'status'` on `post.count` — `Deal` has `status`, **`Post` does
not**. Its real fields are `id, slug, title, body, excerpt, cover, author, seoTitle, seoDesc,
publishedAt, createdAt, updatedAt, tags`. The IST bucketing orders and reads `publishedAt` for the
same reason. One line so the next tick does not rediscover it from a stack trace.

### Ingest pace — the 09-19f dip is closed

```
09-19e  222 / 24h   949 / 7d  ≈ 136/day
09-19f  220 / 24h   917 / 7d  ≈ 131/day   ← single reading outside band
09-19g  226 / 24h   951 / 7d  ≈ 136/day   ← back in band
```

`09-19f` flagged 131 as the first reading outside a 136-137 band and declined to call it a
decline on one sample. **That restraint was correct** — the 7-day window had slid past a busier
day and has since slid back. Closed, not a trend.

### Cursor drift — predicted and reconciled

**205**, exactly the +1 on 204 that the single LONGWAY push predicts, with the +34 before it
accounted for by the `6fa79bd` indiafreestuff batch. Reconciled arithmetic, not an observation.

The cursor stays untouched. An unattended drain dumps **205** messages to the channel at once, and
draining it needs the owner's explicit go-ahead (owner decision #3).

## Fixed inline

**Nothing.** No arm returned a non-zero defect, no endpoint was out of band, prod and DB agreed
exactly. Said plainly rather than manufacturing a repair to fill the section — a tick that finds
nothing wrong is a valid result, and inventing work to look busy would corrupt the one signal this
report exists to carry.

## Rot standing — 29 items

**#29** — backfill verified at 0; **class stays open** (no ingest-path guarantee).

**#4 → 205.** Only the external tg-broadcast cron closes it.

**#11 clear** — every scratch script this window (`_a0919n.mjs`, `_a0919n2.mjs`, `_u0919n.txt`)
deleted in the same Bash call that ran it. The ~200 pre-existing artefacts under `apps/api/` are
untouched (owner decision #5).

**#12** stays half-fixed — CLAUDE.md still documents chunked `/sitemap/deals.xml` routes that prod
404s, while one `/sitemap.xml` serves all **9,640** URLs.

**#21 materially weakened** and unexercised this tick — downgrade or close.

**#22 quiet** — no stale-price repair needed this hour.

Nothing new rotted.

### One new minor item

The audit script assumed a `Post.status` column that does not exist. Cost: one thrown query, no
lost data (the URL list had already been written before the throw). Logged, not rot.

## Open owner decisions — 10

**#7 (persist a reject cache)** is top on frequency, and `telegram-2026-09-19N` made the case
harder: **nine** of fourteen sidebar rows this hour were already-resolved repeats.

**#6 (periodic re-verify sweep)** stays strongest on impact. The 89-row finding an hour ago is
still the argument — **a row can be broken with every column the standing audit checks fully
populated.**

**#3** is at **205** and grows with every push.
