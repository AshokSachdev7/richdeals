# SEO audit + fix: richdeals.in, 2026-09-17

Input: `reports/schema-audit-2026-09-17.md` (W1-W8). This run applied the safe fixes, spot-checked on-page basics on every page type, deployed, and pinged IndexNow.

## Spot-check (prod, before deploy)

| URL | Title len | Desc len | H1 | Robots |
|-----|-----------|----------|----|--------|
| / | 57 | 163 | 1 | index, follow |
| /offers | 62 | 164 | 1 | index, follow |
| /coupons | 62 | 162 | 1 | index, follow |
| /freebies | 54 | 161 | 1 | index, follow |
| /blog | 56 | 156 | 1 | index, follow |
| /blog/qled-vs-oled-vs-mini-led-tv-india-2026 | 69 | 153 | 1 | index, follow |
| /jiomotive-4g-obd-gps-tracker-wi-fi-router-b0clrqmkyr | 61 | 175 | 1 | index, follow |
| /stores/amazon | 64 | 166 | 1 | no meta tag (default index) |
| /stores | 46 | 164 | 1 | index, follow |

All pages return 200 with exactly one H1. `/sitemap.xml` and `/api/deals` both return 200. Minor issues: a few titles run past 60 characters once the ` | RichDeals` suffix is added, and the deal meta description is 175 characters. Neither was changed in this run.

## Fixes applied (commit `b84f726`)

- **W1** `apps/web/src/app/blog/[slug]/page.tsx`: `extractFaq()` now strips `**` and `[text](url)` from answers.
- **W2** `apps/web/src/app/[dealSlug]/page.tsx` and `apps/web/src/lib/site.ts` (`dealItemListSchema`): Offer `validFrom` now comes from `deal.createdAt` instead of render time. Expired deals no longer get `priceValidUntil`.
- **W3** `[dealSlug]/page.tsx`: removed the MRP fallback. A deal with no price gets no Offer.
- **W7** `[dealSlug]/page.tsx`: the last breadcrumb now uses `dealProductName(deal)`.
- `apps/web/src/app/llms.txt/route.ts` and `llms-full.txt/route.ts`: pinned the free-samples guide and `/freebies`. These edits were already made but uncommitted.

## Deploy

- Pushed `c9a0ddf..b84f726` to `origin/master`.
- DO deployment `314e6d10-ea73-4695-a846-0583b5b69156` reached **ACTIVE**.
- Local API restarted on :4000 and returns 200.

## Prod verification (after deploy)

- `/llms.txt` contains `how-to-get-free-samples-freebies-india`: **pass**
- FAQPage JSON-LD on /blog/how-to-port-vodafone-vi-to-airtel-india-2026 and /blog/how-to-activate-airtel-sim-india-2026 has no `**` and no `](`: **pass**
- The last breadcrumb on the jiomotive deal page reads "JioMotive 4G OBD GPS Tracker & Wi-Fi Router" with no "at ₹": **pass**
- The expired tokyo-talkies deal has `validFrom 2026-07-22`, no `priceValidUntil`, and `Discontinued`: **pass**

## IndexNow

29 URLs: `/`, `/offers`, the 20 slugs in `_pushed-slugs-0917.txt`, jiomotive, blog/qled-vs-oled-vs-mini-led-tv-india-2026, `/llms.txt`, `/coupons`, `/freebies` and `/sitemap.xml`.
- api.indexnow.org returned **HTTP 422**.
- The Bing GET fallback returned **HTTP 200 for all 29**.

## Not fixed (open)

- **W4** `dealFaq()` makes generic warranty and EMI claims, and gives a "best price" answer on expired or price-less deals. This conflicts with the real-fields-only rule and needs a copy decision.
- **W5** The homepage, store and category ItemLists nest full Product and Offer objects. Switching to url-only ListItems changes schema on 3 page types.
- **W6** The `/coupons` and `/freebies` feeds are unfiltered because the API `deals.controller.ts` ignores `?type=`. Fixing it needs an API change and an API deploy.
- **W8** Product has no `brand` or `sku`. An ASIN-based `sku` is possible for Amazon deals.
- Minor: some titles are over 60 characters with the brand suffix, and deal meta descriptions are around 175 characters.

## Re-run (~06:30 IST) — commit 4c0911f

**Fixed**
- **FAQPage on 119 blog posts.** FAQ questions written as `**Q?**` lines (not `###`) got no FAQPage JSON-LD and no accordion. `extractFaq()` now turns them into headings inside the FAQ section and removes markdown escapes (`\*199#`) from questions and answers. Checked over all 308 posts: 308/308 now yield FAQ Q&As, with 0 containing leftover `**`, `](`, or escapes. Confirmed on prod: best-usb-c-hubs (5 Q), vi-number-check-code (10 Q).
- **Dead internal link.** Scanned all 1796 post-body links (581 unique). Every hub, store and category route returned 200. The one 404 was a renamed Ant Globe 10 mouse deal (#8996), linked from 2 posts. Both post bodies now point to the current slug.
- Deploy 533ae49d ACTIVE. IndexNow HTTP 200 (8 URLs), Bing 200.

**Still open:** W4, W5, W6, W8, long titles, deal meta descriptions ~175 chars (see above).
