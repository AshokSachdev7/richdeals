# SITEMON + CEO audit — 2026-09-20e (IST)

**All 7 endpoints 200. Every audit arm clean. Nothing fixed inline, because nothing was broken.**

## Endpoints

| Endpoint | Status | Bytes | Time |
|---|---|---|---|
| `/` | 200 | 315,461 | **0.2596 s** |
| `/offers` | 200 | 60,233 | 0.2049 s |
| `/blog` | 200 | 148,710 | 0.7857 s |
| `/sitemap.xml` | 200 | 2,113,476 | 0.3811 s |
| `/feed.xml` | 200 | 49,545 | 0.1714 s |
| `/api/deals` | 200 | 41,851 | 0.1421 s |
| `/llms.txt` | 200 | 14,653 | 0.4643 s |

Sitemap carries **9,701 `<loc>`** (+1 on last tick).

**The `/` 2.016 s flag stays WITHDRAWN.** 0.2596 s is the **third consecutive** non-reproduction.
The single slow read was a cold ISR render, not a regression. Not re-raising it.

## Deal DB

**These counts were measured BEFORE this hour's DEAL-INGEST push.** That tick published **+21**
after this snapshot was taken, so the live numbers are now LIVE ≈ 10,419 and max ≈ 10,766. Reporting
the measured values as measured rather than restating a stale count as current:

| Metric | Value (pre-push) |
|---|---|
| LIVE | 10,398 |
| PENDING_REVIEW | **0** |
| EXPIRED | 259 |
| DRAFT | 0 |
| Max deal id | 10,745 |
| Created last 24 h | 58 |
| Created last 7 d | 722 |
| LIVE with null price | **0** |
| LIVE with null image | **0** |

Each status counted with its own `count()` — `groupBy({by:['status']})` omits zero-row statuses, which
is exactly how a PENDING_REVIEW backlog would hide.

Null-price and null-image are both **0**. No junk to classify, nothing to delist to EXPIRED.

## Blog

318 posts total. **0 coverless, 0 missing seoTitle, 0 missing seoDesc.**

Per day, bucketed in IST (`d.getTime()+19800000`, because Git Bash ignores `TZ`):

| Date (IST) | Posts |
|---|---|
| 09-14 | **4** |
| 09-15 | 3 |
| 09-16 | 3 |
| 09-17 | 3 |
| 09-18 | 3 |
| 09-19 | 3 |
| 09-20 | 2 |

Never 0. 09-14 sits **at** the cap of 4, not over it — compliant, not rot. 09-20 at 2 is inside the
2-3 band with the day still running.

## Broadcast cursor

`.tg-broadcast-cursor.json` → `lastId: 10745`, which **equalled the DB max at read time. Gap 0.**
Fourth self-heal. **Rot #4 stays CLOSED** — the drift is the external cron doing its job, and the
standing rule is to re-read the file rather than carry a stale gap forward.

The 21 deals pushed this hour now sit **above** the cursor. That is the design working, not drift:
they are freshly published and not yet broadcast. **Cursor not drained** — that needs the owner's
explicit go-ahead.

## Git

**0 unpushed commits** at measurement time (before this tick's own two report commits).

## CEO audit summary

| Arm | Result |
|---|---|
| Prod endpoints (7) | all 200 |
| `/` latency | 0.2596 s — 2.016 s flag withdrawn, third non-reproduction |
| Live deal count sanity | 10,398 LIVE, +58 in 24 h — healthy ingest rate |
| Posts/day IST | 4/3/3/3/3/3/2 — never 0, never over 4 |
| Coverless / seo-less posts | 0 / 0 / 0 |
| Deals null price / null image | 0 / 0 — nothing to classify or delist |
| PENDING_REVIEW backlog | 0 |
| Cursor vs DB max | gap 0, fourth self-heal, not drained |
| Unpushed commits | 0 |
| Artifacts | none — terminal + this file only |

### Rot flagged

Nothing new from this tick's own arms. Carried, verified against the DB before flagging:

- **1,602 LIVE deals (15%) carry no MRP, 263 of them from the last 30 days.** Largest data gap on
  the site; it is the concrete target for owner decision #6 (periodic re-verify sweep). Re-grounded:
  every one of those rows has a price, the missing field is `mrp` only.
  **The old "null-discount arm blind spot" framing was wrong and is not being repeated** — the arm's
  0 was correct, because zero rows have an MRP present with a null discount.
- Deal 7637 — EXPIRED candidate, still carried.
- Deal 1536 — slug says ₹299, row says ₹499.
- ~200 scratch leftovers under `apps/api/scripts/` (owner decision #5).
- Doc rot: CLAUDE.md still says "RSS first" while feedburner returns HTTP 000, and still claims
  plain-curl ld+json for non-Amazon while Flipkart PDPs 403 with reCAPTCHA.

## Fixed inline this tick

**Nothing.** Every arm came back clean, so there was no safe rot to fix. An empty fix list on a green
tick is the correct output — inventing work here would only add risk to a healthy system.
