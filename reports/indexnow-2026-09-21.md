# INDEXNOW tick — richdeals.in — 2026-09-21

## Submission

Window: everything created in the **last 6 hours**, pulled from the DB (not from a tick log) so
nothing pushed by an external cron gets missed.

| set | count |
|---|---:|
| LIVE deals created in window | 63 |
| posts published in window | 1 |
| slugs submitted | 64 |
| paths auto-appended by the script (`/`, `/offers`, `/sitemap.xml`) | 3 |
| **URLs in the payload** | **67** |

```
node apps/api/scripts/indexnow-ping.mjs <63 deal slugs> blog/mixer-grinder-500w-vs-750w-vs-1000w-india-2026
DONE: IndexNow -> HTTP 200 for 67 urls
```

**HTTP 200 from `api.indexnow.org`. No 422, so the Bing GET fallback was not needed.**

`/sitemap.xml` is one of the three paths `indexnow-ping.mjs` always prepends, so the sitemap
resubmission the tick asks for is covered by the same call — no separate ping.

## What was in the window

- **10736-10744** (9) — the 09-20 14:26 UTC deal-ingest batch.
- **10745** — a single Cuelinks row (The Man Company gift set, `cbkgrt9gvf2xn6ce` product id).
- **10746-10766** (21) — the 15:58 UTC batch.
- **10767-10788** (22) — the 16:21 / 17:22 / 17:50 / 18:26 UTC batches.
- **10789-10790** (2) — today's TELEGRAM tick (Cello dinner set, Bosch fridge).
- **10791-10798** (8) — today's DEAL-INGEST tick.
- **Post 378** — `mixer-grinder-500w-vs-750w-vs-1000w-india-2026`, published 19:21 UTC.

The post already pinged once through `insert-blog-mdmeta.mjs` at publish time. Resubmitting it here
is a duplicate, not a problem — IndexNow is idempotent and the tick's brief is "newest deal/post
URLs", so the post belongs in the set.

Blog slugs are **not** root-level. They were submitted as `blog/<slug>` so the script builds
`/blog/<slug>`; passing a bare post slug would have pinged a root URL that 404s. Worth remembering
the next time a post lands inside an INDEXNOW window.

## CEO audit

**The last two ticks' pings were already correct and this one overlaps them.** TELEGRAM pinged 5
urls (2 slugs + 3) and DEAL-INGEST pinged 11 (8 + 3). Both those batches are inside the 6h window,
so 10 of the 64 slugs here are a deliberate resubmit. No batch in the window went unpinged at push
time — the freshness rule held on every one.

**Nothing in the window is unreachable.** Every slug came out of the DB with `status:'LIVE'`, so
none of the 63 is an EXPIRED page being fed to a crawler.

**Carried rot, unchanged:**

- **The flat-₹ coupon detector gap is still the highest-value open item** — `/(\d+)% ?(?:off )?[Cc]oupon/`
  cannot see `[Apply ₹1500 Coupon]`. Needs a `₹\s?[\d,]+\s*(?:off\s*)?[Cc]oupon` arm in
  `ingest-common.mjs` before the next ingest tick. Silent under-report, nothing else surfaces it.
- **Blog cadence: IST 09-21 sits at 1 post** (the one in this window), 09-20 closed at 2. Inside the
  hard rule, but the next two CONTENT-SEO ticks need to carry 09-21 to 3.
- **1,603 LIVE deals with no MRP** (owner decision #6).
- A **Bing GET fallback inside `indexnow-ping.mjs`** is still an unshipped safe addition. It has not
  been needed in the last three ticks (200, 200, 200), so it stays a to-do, not a fix.
- **#48** Rogerkart JS redirect. **#47** ONLINE SHOPPING DEALS derivative, 50% dup. **#46**
  CoolzTricks nameless coupon claims. **#45** `data/tg-multi-seen.json` drifts both ways. **#44**
  IFS Flipkart `?rto=` → 403. **#43** CLAUDE.md's "homepage HTML fallback" for a homepage with no
  deal grid.
- Source RSS feedburner HTTP 000 while CLAUDE.md says "RSS first".
- LIVE deal 10031 carries `productId` `ae27f94b3330`, a hex hash.
- ~200 scratch files in `apps/api/scripts/` — owner decision #5.

Scratch hygiene clean — `apps/api/_in0921.cjs` and `_in0921b.cjs` each created and removed in the
same Bash call (34th and 35th); the temp slug list outside the repo removed too. Nothing pushed to
the DB this tick, so no dedup, no bulk call, no cursor touched.
